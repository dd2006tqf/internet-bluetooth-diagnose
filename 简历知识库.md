# 项目面试知识库（基于 eBPF + D-Bus 的多协议内核级网络质量监测平台）

本文档是针对本项目（AI-powered-Network-Diagnostics）技术细节、底层原理、工程架构及工作流的面试应答与知识沉淀库。

---

## 一、 核心概念与工作流机制深度问答

### 1. 什么是“消费者证明”（Consumer Proof）？
- **核心定义**：在规格驱动开发（Spec-Driven Development）中，每当新增、修改一个公开接口、配置项或数据结构（称为 Product Surface，产品表面）时，不能仅凭“我觉得有用”或“单元测试调了一下”就算完成，**必须提供该表面在真实生产环境中有且仅有的实际调用方代码及端到端运行证据**。
- **为什么单元测试不算真实消费者？**
  - 单元测试是开发者为了验证局部逻辑人为编写的孤立调用，它的生命周期局限在测试运行期。
  - 如果一个接口只有单元测试在调，业务路径没有任何代码使用它，它在工程上就是典型的**死代码/孤儿接口（Orphan Surface）**。这会虚增系统维护成本，并在重构时带来伪负担。
- **七大产品表面的最小证明矩阵**：
  1. `internal_api`（内部函数/类）：必须在业务主流程（生产路径）中有真实的调用方。
  2. `external_api`（导出的 C API / 动态库符号）：必须有独立的示例程序或下游应用完成编译、链接并调用成功。
  3. `callback_or_plugin`（回调或插件）：既要有注册逻辑，还要在真实事件发生时被内核/总线成功派发。
  4. `cli`（命令行工具）：提供真实执行的终端命令、退出码（0）及标准输出。
  5. `configuration`（配置项）：必须有进程在运行时真实读取该值，并产生可观测的行为改变（只测 JSON 解析器不算）。
  6. `protocol_or_persistence`（通信协议/持久化）：必须成对验证生产者写入与消费者解析（不能只写不读）。
  7. `build_or_install`（构建安装）：下游依赖能在干净环境下配置、编译并链接该产物。

---

### 2. TDD 四步（RED → GREEN → REFACTOR → REGRESSION）是普遍的技术用语吗？
- **行业背景**：是的，TDD（Test-Driven Development，测试驱动开发）由软件工程大师 Kent Beck 提出，是极限编程（XP）和敏捷开发中**全球通用的标准工程术语**。
- **前三步（经典 TDD 循环）**：
  1. **RED（红灯/先写断言）**：在未写实现前，先写一个失败的测试。**目的**：确保测试本身是有效的，能精准捕捉到该功能缺失的状态，杜绝假阳性。
  2. **GREEN（绿灯/最简实现）**：编写最简单、最克制的生产代码，哪怕硬编码或粗糙实现，只要让测试通过变绿即可。**目的**：防止过度设计，快速形成最小可验证闭环。
  3. **REFACTOR（重构/消除坏味道）**：在测试保持绿灯的保护伞下，优化代码架构、消除重复、提高可读性与性能，外部行为严格保持不变。
- **第四步 REGRESSION（回归测试/本项目的工程演进）**：
  - 在底层系统级开发（如涉及 eBPF 探针、多线程锁、D-Bus 跨进程）中，局部修改极易引发连锁反应（如锁竞争、内核验证器拒绝、破坏其他监控器）。
  - 本工作流将 **REGRESSION** 确立为任务关闭的硬性凭据：每次变更结束后必须触发全量非隔离测试套件执行，确保零历史功能退化。

---

### 3. Evaluator 是如何跑测试命令与落盘证据的？
本项目杜绝“口头声称通过”或“手工截图”，所有证据由自动化 Harness 脚本在受控环境下拦截执行。具体技术链路如下：

1. **会话环境与文件锁拦截**：
   - 评估者调用：`evaluator_check.sh --run --kind test --expect-exit 0 -- <command...>`
   - 脚本通过 `harness_lock.sh` 获取排他锁，校验当前 Git 快照，杜绝并发修改。
2. **基线与指纹双向锚定**：
   - 脚本读取 `evaluation-baseline.json`，核验当前的 `planning_fingerprint` 是否与规划冻结时一致。任何人在实现阶段私自改动 `design.md`，校验立即报错并熔断。
3. **隔离执行与证据生成**：
   - 清空临时的 `verification_workspace`，防止测试被历史残留文件污染。
   - 使用 Node.js 驱动子进程启动测试命令，将命令的 `argv` 数组、执行时机器精确纳秒时钟（`started_at`/`finished_at`）、标准输出/错误统一重定向至临时内存缓存。
4. **输出指纹计算与敏感信息审查**：
   - 对全部输出流计算 `sha256` 摘要：`output_hash="sha256:..."`。
   - 强制跑正则审查，严禁凭据、密钥（Token / Password / Cookie）落入证据文件。
5. **原子落盘到账本（Ledger）**：
   - 脚本将命令及退出码写入 `openspec/changes/<change>/harness/evaluation-command-ledger.json`。
   - 写入采用原子重命名机制（先写入带 PID 和随机数的隐藏临时文件，再调用 `fs.renameSync` 替换），避免进程异常退出留下残缺数据。
   - 最终由 Evaluator 汇总账本，写入单一权威裁决 `evaluation.json`。

---

## 二、 四维职责拆解：简历四大技术板块的分工与边界

### 2.1 简历第 1 点（并发协调中枢）：多线程并发监控

**核心定位**：把 16 路互不相干的物理探测，收敛成一份线程安全、可原子更新、可实时调参的全局状态。

#### 业务职责
| 职责 | 解决的现实灾难 |
|:--|:--|
| **消除数据竞争** | 16 个监控线程与查询/落盘线程同时读写同一份网卡列表，无锁即未定义行为 |
| **消除撕裂读（Torn Read）** | 一个 `NetInfo` 有十几个字段，若更新到一半被读取，外部会看到「RTT 已更新但质量等级还是旧的」 |
| **支撑可运维性** | 现场调试时不能每次改采样周期都重启服务，否则丢失探测状态并断开已订阅客户端 |
| **避免事件风暴** | 若每轮采集都无条件广播信号，10s×16 路会淹没客户端；必须只在指标真正变化时通知 |

#### 代码职责（含真实文件与行号）
1. **统一数据类封装 `NetInfo`**（`server/include/net_info.hpp:63`）
   - 所有监控器产出的原始数字最终归一化进这个类，是「单一事实源」。
   - 字段私有 + `getXxx/setXxx`，后续加「赋值时自动打时间戳」只改一个 setter。
   - **注意：`NetInfo` 本身不加锁**——安全性由外层集合统一提供（锁放在 `WeakNetMgr` 而非每个元素上，避免 N 个元素 N 把锁的开销）。
2. **互斥锁与锁守卫 `WeakNetMgr`**（`server/include/weak_netmgr.hpp:41`）
   ```cpp
   mutable std::mutex iface_mutex_;   ///< 保护接口列表的互斥锁
   std::vector<NetInfo> current_interfaces_;
   ```
   锁封装在方法内部（`server/src/weak_netmgr.cpp:370`）：
   ```cpp
   bool WeakNetMgr::updateRttAndStateSafe(const std::string& host, int timeoutMs) {
       std::lock_guard<std::mutex> lock(mutex_);   // RAII 加锁
       return updateRttAndState(current_interfaces_, host, timeoutMs);
   }
   ```
   - `std::lock_guard`：构造加锁、析构解锁，无论正常返回、提前 return 还是抛异常都必定释放。
   - `mutable`：让 `const` 成员函数也能加锁。
3. **变更检测 `anyChanged` 模式**（`server/src/weak_netmgr.cpp:88-112`）
   - **先比对旧值，只有真正变化才写入并置位 `anyChanged`**，该布尔值一路传回监控线程：
     ```cpp
     // server/src/rtt_monitor.cpp:80
     if (changed && ctx->service) ctx->service->emitChanged("RTT/Quality updated", 0);
     ```
   - 把「每 10 秒无脑广播」变成「只在状态跃迁时广播」。
4. **运行时热调参 `WeakNetConfig`**（`server/include/weaknet_config.hpp:32-91`）
   - **按数据类型选择同步原语**：
     | 数据类型 | 同步机制 | 原因 |
     |:--|:--|:--|
     | `bool` / `uint32_t` | `std::atomic<T>` | 无锁、纳秒级、跨核可见 |
     | `std::string` | `ConfigString`（内含 `std::mutex`） | C++20 前无 `std::atomic<std::string>`，且字符串拷贝非原子 |
   - 写入侧四道关卡（`:242-255`）：白名单校验 → 类型校验 → 区间校验 → 原子提交（任一步失败保持旧值，杜绝半更新）。
   - 读取侧**每轮现读**（`server/src/rtt_monitor.cpp:54-60`）：配置值不在启动时缓存，而是每个循环迭代重新从配置树取——**这就是「无需重启」的全部秘密**。
5. **线程生命周期 `ServerContext`**（`server/include/server.hpp:56-99`）
   - **两级退出机制**：`running` 控制整体退出；`*_stop` 控制单个监控器独立启停（对应 D-Bus 的 `EnableMonitor`/`DisableMonitor`/`RestartMonitor`）。
   - `std::unique_ptr` 管理 `MonitorManager`/`DatabaseManager`/`DbusService`，析构自动释放。
6. **插件依赖排序 `MonitorManager`**（`server/include/monitor_manager.hpp:55-72`）
   - 按插件声明的 `dependencies()` 做拓扑排序后启动，退出时逆序停止。
7. **另两处同步范例**
   - **快照缓存 + 极简临界区**（`server/src/traffic_analyzer.hpp:89-90`）：耗时的 eBPF 读取在锁外完成，只有 `cached_stats_ = stats;` 一行赋值在锁内。
   - **单例的两种形态**：全局单例用 `std::call_once` + `std::once_flag`（`server/src/net_tcp.cpp:63-69`）；线程局部单例用 `static thread_local std::unique_ptr<Looper>`（`server/src/looper.cpp:38`）。

#### 输入输出职责
| 方向 | 内容 | 来源 / 去向 |
|:--|:--|:--|
| 输入 | 16 路监控线程的裸数据 | ICMP RTT、wpa RSSI、Netlink TCP 统计、eBPF Map 快照、BlueZ 属性 |
| 输入 | 热调参请求 | `weaknet-cli set` → D-Bus `SetMonitorParam` → `setMonitorParam(cfg, ...)` |
| 输出 | 统一全局状态快照 | `WeakNetMgr::current_interfaces_` |
| 输出 | 变更事件 | `anyChanged` → `emitChanged()` / `emitSpecificSignal()` → D-Bus 广播 |
| 输出 | 生命周期控制 | `MonitorManager::startAll()/stopAll()`、`ServerContext::running` / `*_stop` |

#### 工程边界职责（它故意不做什么）
| 不做的事 | 为什么 |
|:--|:--|
| 不做实际物理探测 | 单一职责，采数据交给各监控器 |
| 不直接发 D-Bus 消息 | 变更检测只返回 `bool`，信号构造交给 `DbusService` |
| V1 不持久化配置 | `setMonitorParam` 仅改内存态，避免运行期调参污染部署配置 |
| 不保证跨监控器的事务一致性 | 锁粒度是单次更新调用，换取低锁竞争与高吞吐，代价是快照可能来自略有差异的时间点 |

---

### 2.2 简历第 2 点（内核数据面）：8 个 eBPF 程序与 CO-RE

**核心定位**：做外科手术式的内核微观插桩，负责高性能、低开销的深层指标提取与故障根因归因。

#### 业务职责
| 职责 | 说明 | 对应程序 |
|:--|:--|:--|
| **性能的毫秒级切片** | 不是「网页打不开」，而是 TTFB 花了多少毫秒、DNS 解析是 5ms 还是 2000ms 超时 | `http_latency`、`dns_monitor` |
| **故障精确定位** | 把每条流的字节消耗绑定到进程 PID/进程名；直接捕获 `kfree_skb` 输出唯一丢包死因 | `flow_rate`、`skb_drop` |
| **物理链路真实感知** | 即使上层显示已连接，只要音频包间隔 >100ms 即判定为可听的卡顿 | `a2dp_media` |

#### 代码职责：内核态「三不」原则
1. **不分配动态内存**：不能调用 `malloc`，临时变量必须在 512 字节受限栈内解决，确保内核不 OOM。
2. **不陷入死循环**：逻辑线性展开，必须让 Verifier 在加载期 100% 静态判定程序会终止（运行时循环需 `#pragma unroll` 固定边界）。
3. **不直接裸读指针**：全部使用 `BPF_CORE_READ()` 或 `bpf_probe_read_user/kernel`，由内核异常修复表托底，读非法地址只返回 `-EFAULT` 而不触发 Kernel Panic。

#### 代码职责：用户态桥接（Pimpl 模式）
1. **生命周期托管**：`bpf_object__open_file` → `bpf_object__load` → `bpf_program__attach`。
2. **数据摆渡**：`bpf_map_lookup_elem` 周期读取，或注册 `perf_buffer` 回调消费事件。
3. **依赖隔离**：把 `bpf_object*`、`bpf_link*` 完全隐藏进 `.cpp` 的 `Impl` 结构体，防止内核头文件污染外层业务逻辑。

#### 输入输出职责
- **输入源**：kprobe 参数（`PT_REGS_PARM1/2/3` 取出的 `struct sock*`、`struct sk_buff*`、`struct msghdr*`）；tracepoint 参数（`trace_event_raw_*`）。
- **输出产物**：内核内存中的 BPF Map（`LRU_HASH`/`HASH`/`PERF_EVENT_ARRAY`）→ 用户态结构体（`DnsStats`、`ProcessNetStats`、`HttpLatencyStats`）。

#### 工程边界职责
- **能做**：内核协议栈流经的每一个包、每一次重传、每一个系统调用入口的高速处理。
- **不能做**：不能与用户态桌面进程直接对话；拿不到无线射频 RSSI；不管理设备拓扑生命周期。

---

### 2.3 简历第 4 点（多协议控制面）：Netlink / wpa_supplicant / BlueZ

**核心定位**：跨物理介质的系统级控制面集成，负责宏观拓扑感知、物理射频采集与跨协议统筹。

#### 业务职责
| 职责 | 说明 |
|:--|:--|
| **全网卡连接状态与路由感知** | 感知有几张物理网卡、默认网关绑在谁身上、接口 UP/DOWN |
| **无线空中环境质量定级** | 拿到物理层 RSSI（如 -65 dBm），判定空中链路是否临近劣化边缘 |
| **近场外设生态识别与共存分析** | 区分经典蓝牙与 BLE，支撑 2.4GHz Wi-Fi/蓝牙共存干扰根因分析 |
| **无 eBPF 环境的兜底基座** | 在不支持 BPF 的系统上仍能算出核心 TCP 丢包率，服务不瘫痪 |

#### 代码职责：三条系统级通信管道
| 管道 | 文件 | 技术要点 |
|:--|:--|:--|
| **Netlink 通信** | `server/src/net_tcp.cpp` | `socket(AF_NETLINK, SOCK_DGRAM, NETLINK_SOCK_DIAG)`；构造 `inet_diag_req_v2` + `NLM_F_DUMP`；解析 `struct tcp_info` 的 `tcpi_total_retrans` |
| **Unix 域套接字** | `server/src/net_wifiriss.cpp` | `socket(AF_UNIX, SOCK_DGRAM, 0)`；本地 bind `/tmp/wpa_ctrl_<pid>_<iface>`；发送 ASCII 命令 `SIGNAL_POLL`；三重降级 `wpa_ctrl` → `nl80211` → `/proc/net/wireless` |
| **D-Bus IPC 客户端** | `server/src/bt_monitor.cpp` | `dbus_bus_get(DBUS_BUS_SYSTEM)`；调用 `org.bluez` 的 `GetManagedObjects`；按 UUID 判定经典蓝牙 / BLE |

#### 输入输出职责
- **输入源**：内核 Netlink 子系统（`NETLINK_SOCK_DIAG`、`RTM_GETLINK`）；`wpa_supplicant` 守护进程的 UNIX socket；`org.bluez` 的 D-Bus 系统总线。
- **输出产物**：`NetInfo` 列表、`WiFiInfo`（RSSI/信道/SSID）、`std::vector<BtDeviceInfo>` → 经 `WeakNetMgr` 加锁汇入全局状态。

#### 工程边界职责
- **能做**：与系统生态紧密集成，获取操作系统配置、物理信号、设备发现与宏观拓扑。
- **不能做**：采样周期不能太频繁（2~10s 量级），否则高频 D-Bus 调用或反复 Netlink dump 会显著增加系统负荷；拿不到包级时序与内核协议栈瞬间内部状态。

---

### 2.4 三维横向对照：四大板块在系统中的角色

| 维度 | 第 1 点（并发协调中枢） | 第 2 点（eBPF 数据面） | 第 4 点（多协议控制面） |
|:--|:--|:--|:--|
| **所在空间** | 用户态，服务进程内部 | 内核态 | 用户态，与系统守护进程交互 |
| **观察视角** | 协调视角：数据的同步与分发 | 微观视角：单条流、单包、单次重传 | 宏观视角：整块网卡、射频信号、设备拓扑 |
| **核心产物** | 线程安全的全局状态快照 | 内核高性能计数与事件 | 跨介质拓扑与物理参数 |
| **关键机制** | `mutex` / `lock_guard` / `atomic` / `call_once` / `thread_local` | kprobe / tracepoint / BPF Map / CO-RE | Netlink / Unix Socket / D-Bus |
| **性能特性** | 微秒级临界区、无锁读快照 | 零拷贝、纳秒级触发、超低 CPU 开销 | 毫秒级轮询、跨进程 IPC |
| **不可替代性** | 让 16 路数据变成唯一可安全读写的事实源 | **故障根因归因**：代码级丢包原因、进程归属、微秒时延 | **物理环境感知**：天线 RSSI、设备连接拓扑、系统网络基座 |
| **若无此层** | 数据竞争、撕裂读、事件风暴、改配置要重启 | 只能黑盒猜测，看不到内核微观事件 | 拿不到 RSSI、蓝牙拓扑等物理信息 |

**一句话概括三者关系**：第 1 点是**中枢神经**（协调与保护），第 4 点是**感官系统**（感知外部物理世界），第 2 点是**内窥镜**（深入内核看微观机理）——三者共同构成一个对外的统一事实源。

---

### 2.5 关键结论：为什么「用了 eBPF」还要 Netlink / wpa_supplicant / BlueZ？

这是面试最高频的追问，答案是**控制面与数据面双轨协同**：

| 协议 | 控制面（简历第 4 点） | 数据面（简历第 2 点） | 协同价值 |
|:--|:--|:--|:--|
| **以太网/TCP** | Netlink `SOCK_DIAG` 拉取全量 `tcp_info`（含 RTO、cwnd、unacked） | `tcp_retransmit.bpf.c` 在重传瞬间推事件 | Netlink 是**拉模型**（低频全景快照 + 无 BPF 环境兜底），eBPF 是**推模型**（高频突发告警） |
| **Wi-Fi** | `wpa_supplicant` 提供物理层 RSSI（dBm） | `wifi_packet_loss.bpf.c` 统计驱动层 TX BUSY/丢弃 | **eBPF 拿不到 RSSI**（属射频私有控制面）；两者结合可断定「信号弱导致射频层丢包」 |
| **蓝牙** | BlueZ D-Bus 提供设备发现与连接状态 | `a2dp_media.bpf.c` 测量 L2CAP 实际发包间隔 | D-Bus 只知「逻辑连着」；eBPF 才知「物理上还在不在传数据」，攻克伪连接盲区 |

**面试话术**：
> 「第 4 点偏重控制面与拓扑感知，用 Netlink、Unix socket、D-Bus 跨越以太网、Wi-Fi 和蓝牙，拿到宏观连接状态、射频 RSSI 与全量协议栈快照；第 2 点偏重内核数据面与高精度事件，用 8 个 eBPF 探针深入协议栈与网卡驱动，做丢包归因、TTFB 分析与音频卡顿监测。两者结合，既解决 eBPF 拿不到射频物理参数的局限，又弥补传统控制面工具看不清内核微观卡顿的盲区。」

---

### 2.6 线程模型与职责划分（补充）
- **主线程（事件驱动中枢）**：运行 `Looper::run()`，基于 Linux `poll` 监听 D-Bus 系统总线文件描述符与 Self-Pipe 退出管道，负责处理外部 RPC 调用与信号派发。
- **后台监控线程矩阵（16 个独立 Worker）**：各监控器继承 `IMonitorPlugin`，由 `MonitorManager` 统筹依赖关系并启动独立工作线程。
- **数据持久化线程（`history_thread`）**：独立周期性唤醒，持锁抓取状态快照，向本地 SQLite 写入时序历史。

---

## 三、 内核级 eBPF 监控全链路

### 1. 整体架构：内核态探针与用户态消费者
- **内核态（8 个 `.bpf.c` 程序）**：
  - `flow_rate.bpf.c`：进程级网络流量与重传统计（kprobe 挂载 IP/UDP/TCP 关键路径）。
  - `tcp_retransmit.bpf.c`：基于 TCP 4 元组精确追踪连接级重传率。
  - `dns_monitor.bpf.c`：UDP 53 端口解析耗时监控（双通道：kprobe 聚合 + tracepoint 细粒度事件）。
  - `wifi_packet_loss.bpf.c`：网卡驱动与协议栈丢包归因（tracepoint 监控 rx、tx_queue、tx_xmit 驱动返回值）。
  - `http_latency.bpf.c`：明文 HTTP 首字节响应延迟（TTFB）与状态码监控（kprobe entry/return 配对）。
  - `tcp_conn_stats.bpf.c`：基于 kretprobe 监控 `inet_csk_accept` 与 `tcp_close`，统计服务连接时长直方图。
  - `skb_drop.bpf.c`：基于 `kfree_skb` 跟踪内核协议栈丢包原因（`enum skb_drop_reason`）。
  - `a2dp_media.bpf.c`：蓝牙音频数据包间隔与卡顿统计。
- **用户态对接层**：
  - 基于 `libbpf` 库，采用 Pimpl 模式（`DnsMonitor::Impl` 等）隔离底层内核头文件依赖。
  - 核心生命周期五步：`bpf_object__open_file` → `bpf_object__load`（Verifier 校验）→ `bpf_map__fd` → `bpf_program__attach` → 定期 `bpf_map_lookup_elem`。

### 2. 核心机制：CO-RE（一次编译，到处运行）
- **痛点**：Linux 内核结构体（如 `struct sock`、`struct sk_buff`）的内部成员偏移量在不同内核版本下经常变动。传统 BCC 方案必须在目标机器上现场安装内核头文件并调用 Clang 编译，开销极大且不适合嵌入式板端。
- **实现**：
  1. 开发期通过 `bpftool` 从目标内核提取 BTF（BPF Type Format），生成精炼头文件 `vmlinux.h`。
  2. 代码中使用 `BPF_CORE_READ(skb, dev)` 替代裸指针解引用，Clang 编译时记录访问意图（Relocation Tag）。
  3. 板端加载时，`libbpf` 自动读取板端运行内核的 `/sys/kernel/btf/vmlinux`，动态计算并修正指令中的实际偏移量。

---

## 四、 D-Bus IPC 进程间通信与系统集成

### 1. 为什么选择 D-Bus System Bus？
- **系统规范性**：Linux 生态中与系统级守护进程（如 systemd、NetworkManager、BlueZ）通信的标准方式。
- **鉴权隔离**：配合 `/etc/dbus-1/system.d/com.example.WeakNet.conf` 策略文件，能够精准控制不同 UID/用户组的访问权限。
- **功能完备**：天然支持面向对象的 RPC 方法调用（Method Call & Return）与异步广播通知（Signal），无需手写私有 socket 封包/拆包协议。

### 2. 客户端调用与跨语言桥接
- **客户端核心库**：C++ 实现并编译为动态链接库 `libweaknet.so`，底层封装 `libdbus-1` C API。
- **对外 C API**：在 `weaknet_client.h` 中导出 `extern "C"` 纯 C 接口函数（如 `weaknet_get_interfaces`、`weaknet_health_check`），完全抹平 C++ 名字修饰（Name Mangling）和 D-Bus 内部细节。
- **Web 桥接**：Python 后端使用标准库 `ctypes.CDLL` 加载 `libweaknet.so`，将 C 指针缓冲区转化为 Python 原生数据结构，由 FastAPI 提供 RESTful 接口并通过 WebSocket 向前端 ECharts 态势大屏持续推流。

---

## 五、 简历第 3 点深度拆解：D-Bus 服务端通信架构

### 5.1 业务职责：为什么需要这一层？

| 职责 | 解决的现实问题 |
|:--|:--|
| **统一总线交互封装** | libdbus-1 是纯 C API，充满裸指针与手工内存管理；业务代码不应直接接触 |
| **对外提供 RPC 方法** | 让 CLI、C 客户端、Python Web 后端能用**同一种协议**查询任意指标 |
| **消除监控线程与客户端的耦合** | 客户端可能随时、任意频率查询，不能让监控线程为它服务 |
| **事件集中分发** | 十几路监控器产生的状态变化需要统一路由到「内部观察者」与「外部总线订阅者」 |
| **保证监控线程不被通信阻塞** | D-Bus 发送失败/守护进程重启不能让采集线程卡住或停摆 |

### 5.2 代码职责（一）：D-Bus 服务封装与 vtable 注册

**文件**：`server/src/dbus_service.cpp`、`server/include/dbus_service.hpp`

**关键设计难点**：libdbus-1 的消息回调签名是**自由函数**（`DBusHandlerResult f(DBusConnection*, DBusMessage*, void*)`），**不允许捕获 `this`**。而我们要调用的却是成员函数 `handleXxx`。

**解法：静态自由函数 + `user_data` 回传 this**（`dbus_service.cpp:70`）：

```cpp
static DBusHandlerResult MessageHandlerStatic(DBusConnection* conn, DBusMessage* msg, void* user_data) {
    auto* self = reinterpret_cast<DbusService*>(user_data);   // ← 从 user_data 取回 this
    if (!self) return DBUS_HANDLER_RESULT_NOT_YET_HANDLED;
    if (dbus_message_is_method_call(msg, kInterface, kMethodGetInterfaces)) {
        self->handleListInterfaces(conn, msg);
        return DBUS_HANDLER_RESULT_HANDLED;      // ← 已处理，停止继续路由
    }
    if (dbus_message_is_method_call(msg, kInterface, kMethodHealthCheck)) { ... }
    // ...约 29 个方法分支（含别名映射：GetInterfaces 与 ListInterfaces 行为一致）
    return DBUS_HANDLER_RESULT_NOT_YET_HANDLED;  // ← 未匹配，交给其他处理器
}
```

**注册 vtable**（`dbus_service.cpp:199-203`）：
```cpp
bool DbusService::register_on_connection(DBusConnection* conn) {
    static DBusObjectPathVTable vtable{};
    vtable.message_function = &MessageHandlerStatic;
    return dbus_connection_register_object_path(conn, kObjectPath, &vtable, this);  // ← this 作为 user_data
}
```
- **vtable 模式**：一个对象路径对应一张函数表（`DBusObjectPathVTable`），libdbus 收到该路径的方法调用时查表并调用 `message_function`。
- **`this` 的生命周期契约**：`DbusService` 由 `ServerContext` 的 `unique_ptr` 持有，必须比 D-Bus 连接活得久。

### 5.3 代码职责（二）：事件集中分发（观察者模式）

**文件**：`server/src/event_manager.cpp`、`server/include/event_manager.hpp`

**分层结构**：
```
监控线程检测到状态跃迁
        ↓
NetworkEventManager::emitEvent(event)          // event_manager.cpp:138
        ↓
  ┌─────┴─────────────────────────────┐
  │ 1. invokeCallbacks(type, event)   │  ← 内部观察者（进程内回调）
  │ 2. emitSpecificSignal(...)        │  ← 外部总线订阅者（D-Bus 信号）
  └───────────────────────────────────┘
```

`emitEvent` 的真实实现（`event_manager.cpp:138-161`）：
```cpp
void NetworkEventManager::emitEvent(const NetworkEvent& event) {
    invokeCallbacks(event.type, event);                    // ① 走进程内回调

    if (server_ctx_ && server_ctx_->service) {             // ② 走 D-Bus 广播
        std::string signalName = getSignalName(event.type);
        std::string fullMessage = event.source.empty() ? event.message
                                                       : "[" + event.source + "] " + event.message;
        static std::atomic<int32_t> eventCounter{0};       // ③ 跨事件类型递增的原子计数器
        if (event.type == EventType::NetworkQualityChanged && !event.details.empty())
            server_ctx_->service->emitNetworkQualitySignal(fullMessage, event.details, eventCounter++);
        else
            server_ctx_->service->emitSpecificSignal(signalName, fullMessage, eventCounter++);
    }
}
```

三个技术亮点：
1. **`static std::atomic<int32_t> eventCounter{0}`**：函数内的静态原子变量。接收方可用它判断事件先后顺序与去重——这是一条容易被忽略但很有价值的工程细节。
2. **双重消费者模型**：进程内回调（例如质量评估器要监听 RTT 变化重算评分）与进程外 D-Bus 订阅者（Web 前端、CLI）**共享同一个事件源**，但互不阻塞。
3. **解耦设计**：`EventManager` 不直接持有各监控器的指针，监控器也不必知道谁在监听——典型的观察者模式（发布-订阅）。

**回调注册 API**（`event_manager.hpp:104/110`）：`registerCallback(EventType, EventCallback)` / `unregisterCallback(EventType)`，内部按 `EventType` 维护 7 组回调向量，由 `cb_mutex_` 保护。

### 5.4 代码职责（三）：带重试的同步信号发送

**文件**：`server/src/dbus_service.cpp:215-261`（`sendSignalInternal`）

```cpp
bool DbusService::sendSignalInternal(const std::string& signalName,
                                    const std::vector<std::pair<int, const void*>>& args,
                                    int32_t counter) {
    if (!ctx_ || !ctx_->connection) return false;

    const int max_retries = 3;
    const int retry_delay_ms = 100;

    for (int attempt = 0; attempt < max_retries; ++attempt) {
        DBusMessage* signal = dbus_message_new_signal(kObjectPath, kInterface, signalName.c_str());
        if (!signal) return false;                      // ← 编程错误，不重试

        DBusMessageIter iter;
        dbus_message_iter_init_append(signal, &iter);
        for (const auto& [type, value] : args) {
            if (!dbus_message_iter_append_basic(&iter, type, value)) {
                dbus_message_unref(signal);
                return false;                           // ← 编程错误，不重试
            }
        }

        bool ok = dbus_connection_send(ctx_->connection, signal, nullptr);
        dbus_connection_flush(ctx_->connection);        // ← 立即刷出 socket 缓冲区
        dbus_message_unref(signal);                     // ← 释放消息引用，防内存泄漏

        if (ok) return true;

        LOG_WARNING(... << "attempt " << attempt + 1 << " failed, retrying in " << retry_delay_ms << "ms");
        if (attempt < max_retries - 1)
            std::this_thread::sleep_for(std::chrono::milliseconds(retry_delay_ms));
    }
    LOG_ERROR(... << "all " << max_retries << " attempts failed");
    return false;
}
```

**重试策略的精妙之处**：
- **区分可重试与不可重试失败**：`dbus_message_new_signal` 失败、参数追加失败属于**编程错误**（参数类型写错等），重试一万次也不会成功，直接返回；只有 `dbus_connection_send` 失败才是**通信抖动**，才重试。
- **`dbus_connection_flush`**：libdbus 内部有发送队列，`send` 只是入队，`flush` 才真正写入 socket。不 flush 会导致信号延迟到下次事件循环才发出。
- **`dbus_message_unref`**：libdbus 是手工引用计数模型（无 RAII），每次 `new_*` 都必须配对 `unref`，否则内存泄漏。注意这里创建成功、追加失败、发送成功三条路径都有 `unref`。

**并发保护 `send_mutex_`**（`dbus_service.hpp:17-18` 注释点明）：
> *`send_mutex_` 保护对同一 DBusConnection 的并发信号发射写操作（libdbus-1 的部分内部结构在多线程写入下不安全）*

所以 `emitSpecificSignal`（`:426`）和 `emitChanged`（`:254`）入口第一行都是：
```cpp
std::lock_guard<std::mutex> lock(send_mutex_);
```

**`emitChanged` 与 `emitSpecificSignal` 的差异**（`dbus_service.cpp:263-274`）：
```cpp
bool DbusService::emitChanged(const std::string& message, int32_t counter) {
    std::lock_guard<std::mutex> lock(send_mutex_);
    const char* s = message.c_str();
    std::vector<std::pair<int, const void*>> args = {
        {DBUS_TYPE_STRING, &s}, {DBUS_TYPE_INT32, &counter}
    };
    bool ok = sendSignalInternal(kSignalChanged, args, counter);
    if (ok) {   // ← 仅在信号发送成功时才持久化，避免文件与 D-Bus 状态不一致
        ChangedPayload payload{message, counter};
        std::string err;
        serializeChangedPayloadToFile(payload, kSignalSerializedFile, &err);
    }
    return ok;
}
```
「**仅在成功时持久化**」这一条是有意为之的一致性设计——先确保外部可见，再落盘，避免出现「文件里有但总线上从未发过」的幽灵状态。

### 5.5 如何保证「监控线程不被通信阻塞」？

这是简历里那句话的技术落点，可以从四个角度回答：

| 手段 | 实现位置 | 效果 |
|:--|:--|:--|
| **主线程独占事件循环** | `server/src/looper.cpp:104` | 所有 D-Bus 收发都在 Looper 线程，监控线程完全不参与总线读写 |
| **发送侧串行化而非阻塞采集** | `send_mutex_` + 3 次重试上限 | 极端情况下最多阻塞 `3 × 100ms = 300ms`，且失败后返回 `false` 而不抛出异常 |
| **快照语义，不在锁内做 I/O** | `handleListInterfaces` 只加锁拷贝 + 序列化 | RPC 处理耗时在毫秒级，不会长时间霸占总线 |
| **事件驱动而非轮询推送** | `anyChanged` 门控 | 无变化时完全不产生总线流量 |

**`Looper` 的事件循环实现**（`server/src/looper.cpp:104-155`）：
```cpp
void Looper::run(ServerContext* ctx) {
    if (pipe(s_stop_pipe) < 0) { ... 降级到 timeout 模式 ... }
    else {
        for (int& fd : s_stop_pipe) {          // 两端都设 O_NONBLOCK
            const int flags = fcntl(fd, F_GETFL, 0);
            if (flags >= 0) (void)fcntl(fd, F_SETFL, flags | O_NONBLOCK);
        }
    }

    struct sigaction sa = {};
    sa.sa_handler = looper_signal_handler;
    sa.sa_flags = 0;                            // ← 不设 SA_RESTART
    sigaction(SIGTERM, &sa, nullptr);
    sigaction(SIGINT, &sa, nullptr);

    int dbus_fd = -1;
    dbus_connection_get_socket(conn_, &dbus_fd);

    struct pollfd fds[2];
    fds[0].fd = dbus_fd;       fds[0].events = POLLIN;   // D-Bus 消息 fd
    fds[1].fd = s_stop_pipe[0]; fds[1].events = POLLIN;  // signal pipe 读端

    while (ctx->running.load()) { ... }
}
```

**self-pipe trick（自管道技巧）详解**——这是 Linux 信号处理的经典手法：

```
问题：signal handler 里只能设置 volatile 标志，
      但 dbus_connection_read_write_dispatch / poll 会阻塞在 fd 上，
      标志变了 poll 也不会返回 → 收不到退出信号。

解法：创建一对 pipe。
      signal handler 只做一件事：向写端 write 1 字节（write 是 async-signal-safe 的）。
      poll 同时监听读端 → 管道一有数据，POLLIN 立即触发 → 退出循环。
```

`looper_signal_handler`（`looper.cpp:61-66`）：
```cpp
static void looper_signal_handler(int signum) {
    if (s_stop_pipe[1] >= 0) {
        char c = 1;
        (void)write(s_stop_pipe[1], &c, 1);   // async-signal-safe
    }
}
```
- **async-signal-safe 约束**：signal handler 运行在异步上下文，只能调用 `write`、`_exit`、`signal` 等少数函数。**绝不能在 handler 里调用 `malloc`、`printf`、`mutex_lock`**——它们内部可能持有锁，而信号可能正好打断锁持有者，导致自死锁。
- **两端 O_NONBLOCK 的原因**（代码注释写得很详细）：读端非阻塞是为了排空循环 `while(read(...)>0)` 能正常结束（否则清空后第二次 read 会永久挂起）；写端非阻塞是为了 handler 中的 write 不因管道满而阻塞。

### 5.6 端到端实例：从 Wi-Fi 劣化到 Web 大屏刷新

```
① 采集：rssi_monitor.cpp 线程经 wpa_supplicant 拿到 RSSI = -88 dBm
        ↓
② 汇聚：WeakNetMgr::updateWifiRssi() 持 iface_mutex_ 写入 NetInfo，返回 anyChanged=true
        ↓
③ 分发：NetworkEventManager::emitEvent(NetworkQualityChanged)
        ├─ invokeCallbacks()  → 进程内观察者（如质量评估器重算总分）
        └─ emitSpecificSignal() / emitNetworkQualitySignal()
                ↓ 持 send_mutex_，构造信号，3 次重试 + flush
④ 总线：dbus-daemon 广播到所有订阅者
        ↓
⑤ 客户端：client/client.cpp 的过滤器收到 signal → 分发给用户注册的 C 回调
        ↓
⑥ 桥接：weaknet_bridge.py（ctypes 加载 libweaknet.so）
        ↓
⑦ 后端：app.py 的 /api/interfaces 或 WebSocket 推送
        ↓
⑧ 前端：app.js fetch → ECharts 折线图刷新，卡片状态由绿变红
```

### 5.7 代码观察：一处文档与实现不一致（诚实记录）

`server/include/event_manager.hpp:88-89` 的头文件注释写着：

> *设计注意：回调中若嵌套调用 emitEvent 会产生锁重入，当前实现在 emitEvent 中先拷贝回调向量再解锁，避免回调中触发新事件时的死锁。*

但实际实现（`server/src/event_manager.cpp:259-298`）是**持锁遍历调用回调**：

```cpp
void NetworkEventManager::invokeCallbacks(EventType type, const NetworkEvent& event) {
    std::lock_guard<std::mutex> lock(cb_mutex_);     // ← 全程持锁
    switch (type) {
        case EventType::InterfaceChanged:
            for (const auto& callback : interface_callbacks_) {
                callback(event);                     // ← 在锁内调用回调
            }
            break;
        ...
    }
}
```

而 `:256-257` 的注释又明确承认了这一点：
> *@note 持 cb_mutex_ 期间调用回调；回调内不得再次调用 registerCallback/unregisterCallback，否则会重入死锁*

**结论**：实现采用「持锁调用 + 约定回调不得重入」，头文件那段「先拷贝再解锁」的描述与当前代码不符，属于文档滞后。面试时若被追问死锁问题，正确答法是：**当前实现靠文档约定规避重入，更稳妥的工业做法是「拷贝回调向量 → 释放锁 → 无锁遍历调用」（Copy-and-Release 模式）。**

---

### 5.8 D-Bus 服务标识与接口清单

```cpp
服务名:   com.example.WeakNet
对象路径: /com/example/WeakNet
接口名:   com.example.WeakNet
总线:     System Bus（策略见 /etc/dbus-1/system.d/com.example.WeakNet.conf）
```

| 方法分类 | 代表方法 | 作用 |
|:--|:--|:--|
| 示例/健康 | `Get`、`HealthCheck` | 连通性验证、综合质量评估 |
| 网络查询 | `GetInterfaces`（别名 `ListInterfaces`）、`Ping` | 网卡列表、连通性探测 |
| 蓝牙 | `GetBluetoothDevices`、`GetBluetoothAdapter`、`GetBluetoothAudioQuality` | 设备列表、适配器状态、A2DP 音频质量融合诊断 |
| 根因诊断 | `GetCoexistenceConflict`、`GetEbpfMonitorHealth` | 2.4GHz 共存干扰 RCA、eBPF 监控器健康快照 |
| 运行管控 | `SetMonitorParam`、`GetMonitorParam`、`ListMonitors`、`GetMonitorStatus`、`EnableMonitor`、`DisableMonitor`、`RestartMonitor`、`SaveMonitorOverrides` | 热调参、启停、状态查询、配置持久化 |
| 历史数据 | `GetHistory` | 查询 SQLite 持久化历史快照 |

**信号清单**：`Changed`、`InterfaceChanged`、`ConnectionModeChanged`、`NetworkQualityChanged`、`BluetoothDeviceChanged`、`TcpLossRateChanged`、`RttChanged`、`RssiChanged`

---

## 六、 D-Bus 架构函数来源清晰划分表（系统/库定义 vs 本项目自定义）

在面试和代码走查中，最容易让初学者混淆的就是：**“到底哪些是 Linux / libdbus 提供的底层标准函数，哪些是我们自己写的业务封装代码？”**

下面按**库定义（外部提供）**与**本项目自定义（我们编写）**对全链路关键符号做出清晰界定：

### 6.1 外部库 / Linux 操作系统提供的接口与数据结构

这些符号均来自官方标准头文件（`<dbus/dbus.h>`、`<poll.h>`、`<signal.h>` 等），由操作系统或 `libdbus-1` 动态链接库提供，我们是**使用者**：

| 符号 / 函数名 | 定义来源（头文件 / 库） | 官方功能与机制说明 |
|:---|:---|:---|
| `dbus_bus_get(DBUS_BUS_SYSTEM, ...)` | `libdbus-1` (`<dbus/dbus.h>`) | 连接 Linux 系统级 D-Bus 总线（System Bus），返回共享连接指针 `DBusConnection*`。 |
| `dbus_bus_request_name(...)` | `libdbus-1` (`<dbus/dbus.h>`) | 向 `dbus-daemon` 注册并抢占唯一的知名服务名称（如 `com.example.WeakNet`）。 |
| `dbus_connection_register_object_path()` | `libdbus-1` (`<dbus/dbus.h>`) | 将特定 Object Path 与路由虚表结构 `DBusObjectPathVTable` 绑定，建立方法路由树。 |
| `DBusObjectPathVTable` | `libdbus-1` (`<dbus/dbus.h>`) | C 语言结构体，包含 `message_function` 成员，用于指定接收到 RPC 调用时的分发函数。 |
| `dbus_message_new_method_call(...)` | `libdbus-1` (`<dbus/dbus.h>`) | 客户端函数：在堆上分配并构造一个 `METHOD_CALL` 类型的 RPC 请求报文对象。 |
| `dbus_message_new_method_return(...)` | `libdbus-1` (`<dbus/dbus.h>`) | 服务端函数：根据接收到的请求报文构造对应的成功应答（`METHOD_RETURN`）报文。 |
| `dbus_message_new_signal(...)` | `libdbus-1` (`<dbus/dbus.h>`) | 服务端函数：构造一个广播类型的 `SIGNAL` 报文对象。 |
| `dbus_message_iter_init(...)`<br>`dbus_message_iter_init_append(...)`<br>`dbus_message_iter_append_basic(...)`<br>`dbus_message_iter_open_container(...)` | `libdbus-1` (`<dbus/dbus.h>`) | **D-Bus 参数打包/解包迭代器系列函数**。按照 D-Bus 二进制传输规约（Type Signature），序列化/反序列化字符串、整型、浮点型或嵌套数组（ARRAY）。 |
| `dbus_connection_send(...)` | `libdbus-1` (`<dbus/dbus.h>`) | 异步投递报文到连接的内部发送队列（注意：仅入队，不保证立刻发出）。 |
| `dbus_connection_flush(...)` | `libdbus-1` (`<dbus/dbus.h>`) | 强制刷新底层 socket 发送缓冲区，使队列中的报文立刻通过网络写出。 |
| `dbus_connection_send_with_reply_and_block()` | `libdbus-1` (`<dbus/dbus.h>`) | 客户端同步阻塞调用函数。发送请求后阻塞当前线程，直至收到应答或超时（如 5000ms）。 |
| `dbus_message_unref(...)` | `libdbus-1` (`<dbus/dbus.h>`) | 递减报文对象的引用计数。因为纯 C 库没有析构函数，必须配对调用释放堆内存。 |
| `dbus_connection_get_socket(...)` | `libdbus-1` (`<dbus/dbus.h>`) | 提取 D-Bus 内部持有的底层 Linux 套接字文件描述符（fd），供 `poll` 统一多路复用监听。 |
| `dbus_connection_read_write_dispatch(...)` | `libdbus-1` (`<dbus/dbus.h>`) | 驱动 D-Bus 状态机：从 socket 读写数据，并将解析后的消息分发给注册的回调。 |
| `poll(struct pollfd*, ...)` | POSIX 系统调用 (`<poll.h>`) | Linux 多路复用 I/O，同时监控 D-Bus fd 和自管道退出的读就绪事件。 |
| `pipe(int pipefd[2])`<br>`fcntl(..., O_NONBLOCK)` | Linux 系统调用 (`<unistd.h>`, `<fcntl.h>`) | 创建无名管道并置为非阻塞模式，用于构建异步信号安全的 Self-pipe trick。 |
| `sigaction(SIGTERM/SIGINT, ...)` | POSIX 系统调用 (`<signal.h>`) | 安装进程终止信号的处理程序。 |

---

### 6.2 本项目自定义的接口、类与核心封装

这些完全是**我们自己团队编写的工程代码**，分布在 `server/` 和 `client/` 源码中：

| 符号 / 类 / 函数名 | 文件位置 | 我们编写的具体实现逻辑与职责 |
|:---|:---|:---|
| `class DbusService` | `server/include/dbus_service.hpp`<br>`server/src/dbus_service.cpp` | **D-Bus 服务主类**。封装所有 libdbus 交互细节，持有 `ServerContext`，提供方法注册与信号发送入口。 |
| `MessageHandlerStatic(...)` | `server/src/dbus_service.cpp:70` | **静态网关路由函数**。作为 D-Bus C 回调，把 `void* user_data` 强转为 `DbusService*`，将 29 种 RPC 路由给成员函数。 |
| `DbusService::handleListInterfaces(...)`<br>`DbusService::handleHealthCheck(...)`<br>`DbusService::handleSetMonitorParam(...)` | `server/src/dbus_service.cpp` | **具体业务 RPC 响应方法**。从弱网管理器拿快照、转 JSON，再打包回复给客户端。 |
| `DbusService::replyStringArray(...)` | `server/src/dbus_service.cpp:341` | 业务辅助函数。递归使用 libdbus 迭代器将 `vector<string>` 序列化为 D-Bus 字符串数组。 |
| `DbusService::sendSignalInternal(...)` | `server/src/dbus_service.cpp:215` | **判别式重试信号发送核心**。实现了 3 次重试、100ms 避退、参数类型编程错误立即返回的容错机制。 |
| `DbusService::emitSpecificSignal(...)`<br>`DbusService::emitChanged(...)` | `server/src/dbus_service.cpp` | 外部信号发射统一接口，内部加 `send_mutex_` 互斥锁保障多线程并发发信号的绝对串行安全。 |
| `class Looper` | `server/include/looper.hpp`<br>`server/src/looper.cpp` | **单例主事件驱动循环**。独占主线程管理 `poll` 与 `read_write_dispatch`，内嵌 Self-pipe trick。 |
| `looper_signal_handler(int)` | `server/src/looper.cpp:61` | 静态信号处理函数。只向管道写 1 字节（异步信号安全），秒级唤醒阻塞在 `poll` 上的主事件循环。 |
| `class NetworkEventManager` | `server/include/event_manager.hpp`<br>`server/src/event_manager.cpp` | **单例观察者总线**。管理 7 组事件类型的回调数组，统一分发内部进程回调与外部 D-Bus 信号广播。 |
| `NetworkEventManager::emitEvent(...)` | `server/src/event_manager.cpp:138` | 事件派发中继。内部递增静态原子变量 `eventCounter`，驱动 `DbusService` 发送对应信号。 |
| `weaknet_get_interfaces(...)`<br>`weaknet_health_check(...)`<br>`weaknet_set_monitor_param(...)` | `client/weaknet_client.h`<br>`client/client.cpp:1158` | **对外公开纯 C API**。使用 `extern "C"` 导出符号，内部转调客户端单例，完全屏蔽 C++ 语法与 D-Bus 细节。 |
| `class WeakNetClient` | `client/client.cpp:48` | **客户端 C++ 实现核心**。封装系统总线连接、请求构造、等待应答、反序列化解析及错误码转换。 |
| `setMonitorParam(...)`<br>`splitMonitorKey(...)` | `server/include/weaknet_config.hpp:239-255` | **热调参校验引擎**。执行点分键解析、白名单匹配、类型转换、区间校验并原子提交内存修改。 |
| `class ConfigString` | `server/include/weaknet_config.hpp:32` | 针对字符串的并发安全封装类，内嵌 `mutable std::mutex` 保证参数热更新时的读写一致性。 |
| `kBusName`, `kObjectPath`, `kInterface`, `kMethodXxx`, `kSignalXxx` | `server/include/common.hpp` | **项目协议常量定义**。集中声明所有总线名、路径名、29 个方法名及 8 个信号名字符串常量。 |

---

### 6.3 官方库函数六大生命周期全景详解（深挖每一个系统级 API）

为了彻底理清官方 `libdbus-1` 动态库与 Linux 操作系统提供的函数调用细节，我们将整个项目中使用到的官方 C 原语按**六大生命周期阶段**逐一详解：

#### 1. 总线生命周期与连接管理（Connection Lifecycle）
*负责与操作系统底层的 `dbus-daemon` 守护进程建立物理通道，并绑定事件循环。*
- **`dbus_bus_get(DBusBusType type, DBusError* error)`**（`<dbus/dbus.h>`）：
  - **调用处**：`server/src/server.cpp:129`、`client/client.cpp:64`。
  - **机制**：传入 `DBUS_BUS_SYSTEM` 连接至 Linux 系统级总线（Unix Domain Socket 地址通常为 `/var/run/dbus/system_bus_socket`），返回全局共享的 `DBusConnection*` 连接句柄。
- **`dbus_bus_request_name(conn, name, flags, error)`**（`<dbus/dbus.h>`）：
  - **调用处**：`server/src/server.cpp:138`。
  - **机制**：向系统守护进程争抢知名服务名称（`com.example.WeakNet`）。传入 `DBUS_NAME_FLAG_REPLACE_EXISTING` 标志，若抢占成功，所有发往该域名的请求均会被总线路由到当前进程。
- **`dbus_connection_get_socket(conn, int* fd)`**（`<dbus/dbus.h>`）：
  - **调用处**：`server/src/looper.cpp:140`。
  - **机制**：从不透明的 `DBusConnection` 结构体中直接抽取底层 Linux 套接字的文件描述符（`fd`）。**核心价值**：拿到原生 fd 后，才能将其交由 Linux 原生的 `poll()` 统一管理，与自管道一起做 I/O 多路复用。
- **`dbus_connection_read_write_dispatch(conn, timeout_ms)`**（`<dbus/dbus.h>`）：
  - **调用处**：`server/src/looper.cpp:175`。
  - **机制**：驱动 D-Bus 内部状态机。尝试从 socket 读写数据流，完成报文边界切分，并将解析出的请求消息分发（dispatch）给绑定的回调函数。

#### 2. 对象路径与路由注册（Object Path & VTable）
*负责在服务内部构建面向对象的路由树，将特定的 URL 式路径与 C 语言回调绑定。*
- **`dbus_connection_register_object_path(conn, path, vtable, user_data)`**（`<dbus/dbus.h>`）：
  - **调用处**：`server/src/dbus_service.cpp:202`。
  - **机制**：将对象路径（`/com/example/WeakNet`）注册到连接上，绑定 `DBusObjectPathVTable` 虚函数表结构，并将 `DbusService` 的 `this` 指针作为 `user_data` 存入。
- **`DBusObjectPathVTable`**（`<dbus/dbus.h>`，官方结构体）：
  - **调用处**：`server/src/dbus_service.cpp:200`。
  - **机制**：内含函数指针 `message_function`，libdbus 拦截到发往该路径的消息时，会自动触发该函数指针。

#### 3. 报文创建与类型识别（Message Creation & Inspection）
*负责在堆内存上分配不同类型的 D-Bus 协议帧结构体（`DBusMessage`）。*
- **`dbus_message_new_method_call(bus_name, path, iface, method)`**（`<dbus/dbus.h>`）：
  - **调用处**：`client/client.cpp:114`、`:868`。
  - **机制**：客户端构造 RPC 请求报文（`DBUS_MESSAGE_TYPE_METHOD_CALL`），自动生成事务序列号（Serial Number）。
- **`dbus_message_new_method_return(method_call_msg)`**（`<dbus/dbus.h>`）：
  - **调用处**：`server/src/dbus_service.cpp:342`、`:404`。
  - **机制**：服务端根据接收到的请求消息创建应答报文（`DBUS_MESSAGE_TYPE_METHOD_RETURN`），底层自动复制请求的 Serial 作为 Reply-Serial，实现请求与应答的准确配对。
- **`dbus_message_new_signal(path, iface, name)`**（`<dbus/dbus.h>`）：
  - **调用处**：`server/src/dbus_service.cpp:224`。
  - **机制**：服务端构造广播信号报文（`DBUS_MESSAGE_TYPE_SIGNAL`），无须指定目标客户端，广播至系统总线。
- **`dbus_message_new_error(msg, error_name, error_message)`**（`<dbus/dbus.h>`）：
  - **调用处**：`server/src/dbus_service.cpp:301`。
  - **机制**：服务端处理异常时（如参数非法），构造错误回执报文，通知客户端失败原因。
- **`dbus_message_is_method_call(msg, iface, method)`**（`<dbus/dbus.h>`）：
  - **调用处**：`server/src/dbus_service.cpp:73-184`。
  - **机制**：快速匹配函数。校验接收到的报文是否为特定接口下的特定方法名。

#### 4. 参数序列化与反序列化（Type Marshalling / Iterators）
*D-Bus 采用强类型二进制规约（Type Signature），必须通过游标迭代器按顺序编解码。*
- **`dbus_message_iter_init(msg, &iter)`** / **`dbus_message_iter_init_append(msg, &iter)`**（`<dbus/dbus.h>`）：
  - **调用处**：`client/client.cpp:140`（读）、`server/src/dbus_service.cpp:231`（写）。
  - **机制**：分别初始化读游标与写游标。
- **`dbus_message_iter_append_basic(&iter, type, &value)`**（`<dbus/dbus.h>`）：
  - **调用处**：`server/src/dbus_service.cpp:235`、`:409`。
  - **机制**：序列化压入单值基础数据类型（如 `DBUS_TYPE_STRING`、`DBUS_TYPE_INT32`）。
- **`dbus_message_iter_get_basic(&iter, &value)`**（`<dbus/dbus.h>`）：
  - **调用处**：`client/client.cpp:154`。
  - **机制**：从读游标中提取出当前位置的基础类型数值或字符串指针。
- **`dbus_message_iter_get_arg_type(&iter)`** / **`dbus_message_iter_next(&iter)`**（`<dbus/dbus.h>`）：
  - **调用处**：`client/client.cpp:141`、`:155`。
  - **机制**：获取当前字段的类型签名，并将游标推进到下一个参数。
- **`dbus_message_iter_open_container(...)`** / **`dbus_message_iter_close_container(...)`**（`<dbus/dbus.h>`）：
  - **调用处**：`server/src/dbus_service.cpp:350`、`:361`。
  - **机制**：开启 / 闭合一个复杂容器类型的构造（如字符串数组 `DBUS_TYPE_ARRAY` 或键值字典 `DICT_ENTRY`）。
- **`dbus_message_iter_recurse(&iter, &sub_iter)`**（`<dbus/dbus.h>`）：
  - **调用处**：`client/client.cpp:149`。
  - **机制**：递归进入复合容器内部，获取子迭代器遍历内部元素。

#### 5. 消息投递与物理刷盘（Transmission & Socket Flush）
- **`dbus_connection_send(conn, msg, &serial)`**（`<dbus/dbus.h>`）：
  - **调用处**：`server/src/dbus_service.cpp:242`。
  - **机制**：将序列化完毕的报文压入 `DBusConnection` 的内存发送队列中。**注意：这只是内存队列投递，并未立刻到达网络！**
- **`dbus_connection_flush(conn)`**（`<dbus/dbus.h>`）：
  - **调用处**：`server/src/dbus_service.cpp:243`、`:365`。
  - **机制**：**强制阻塞刷新**。把内存发送队列中积攒的所有字节立即通过系统调用写入底层的 socket 缓冲区。缺少此步骤会导致信号在高频场景下发生未知延迟滞留。
- **`dbus_connection_send_with_reply_and_block(conn, msg, timeout_ms, &err)`**（`<dbus/dbus.h>`）：
  - **调用处**：`client/client.cpp:124`、`:883`。
  - **机制**：客户端最核心的同步 RPC 调用原语。发送请求后阻塞挂起当前调用线程，直至收到总线应答报文或超时（如 5000ms），唤醒后直接返回 `DBusMessage*` 应答对象。

#### 6. 手工内存生命周期管控（Manual Reference Counting）
*由于 libdbus-1 是纯 C 语言库，不存在 C++ 的 RAII 自动析构机制，堆对象必须严格配对释放。*
- **`dbus_message_unref(msg)`**（`<dbus/dbus.h>`）：
  - **调用处**：`server/src/dbus_service.cpp:244`、`client/client.cpp:125`。
  - **铁律机制**：报文对象内部采用引用计数。通过 `new_method_call`、`new_method_return`、`new_signal` 创建的报文初始引用计数为 1；使用完毕后必须显式调用 `unref` 递减至 0 触发释放。漏调一次即发生几十字节至几千字节的内存泄漏，最终导致守护进程 OOM 崩溃。
- **`dbus_error_init(&err)`** / **`dbus_error_free(&err)`** / **`dbus_error_is_set(&err)`**（`<dbus/dbus.h>`）：
  - **调用处**：`client/client.cpp:121`、`:127`、`:129`。
  - **机制**：管理 `DBusError` 结构体的生命周期，记录系统调用失败时的错误域名（Error Name）与详细错误提示字符串。

---

### 6.4 简历对应提炼：如何一句话向面试官阐述该架构？

> **“在我们的服务端通信架构中，所有的进程间通信与状态机调度都清晰地划分为两层：**  
> **底层直接基于 Linux 官方 `libdbus-1` C 动态库，我们精准使用了 `dbus_connection_send_with_reply_and_block`、`dbus_message_iter_*` 等核心 C 原语进行套接字传输与高效二进制参数序列化；**  
> **而上层架构则完全由我们自主研发与面向对象封装：包括通过 `MessageHandlerStatic` 解决 C 回调无 `this` 指针的网关路由、基于 Self-pipe 与 `poll` 实现无死锁高可靠的 `Looper` 事件循环、在 `sendSignalInternal` 中自主实现的判别式 3 次重试容错机制，以及通过 `WeakNetConfig` 实现的 4 重校验原子内存热调参引擎。底层靠标准原语保证协议互通，上层靠自研封装保证系统健壮与零重启可运维。”**

---

## 七、 深度技术补全（简历 5 大板块高频面试深挖点）

### 7.1 【对应第 1 点】动态热调参底层解析引擎与参数全景清单

在实际运维与弱网诊断中，系统支持在**不重启监控服务的前提下，通过 CLI 实时修改 16 个监控模块的采样周期、超时门限、探测目标与底层 BPF 字节码路径**。

#### 1. 动态调参底层技术架构链路
```
[运维人员执行 CLI]
  weaknet-cli set rtt.interval 5s
       │
       ▼ ① 客户端打包 IPC 请求 (client/client.cpp:868)
  构造 D-Bus 方法调用: SetMonitorParam("rtt.interval", "5s")
       │ (Linux D-Bus System Bus 系统总线传递)
       ▼ ② 服务端网关接收并解析 (server/src/dbus_service.cpp:138/484)
  MessageHandlerStatic() 路由到 handleSetMonitorParam()
       │
       ▼ ③ 核心校验与原子内存提交 (server/src/weaknet_config.cpp:540-639)
  setMonitorParam() 执行 4 重防御:
    ├─ 1. 点分键路由: splitMonitorKey("rtt.interval") -> monitor="rtt", field="interval"
    ├─ 2. 时间单位解析: parseDurationMs("5s") -> 5000 ms (防整型乘法溢出)
    ├─ 3. 白名单与区间安全校验: checkRange(5000, 100ms, 600000ms)
    └─ 4. 原子提交内存: cfg->rtt.interval_ms.store(5000) (零磁盘IO，纯内存态)
       │
       ▼ ④ 监控线程下一轮循环秒级感知 (server/src/rtt_monitor.cpp:55-85)
  while (ctx->running.load() && !ctx->rtt_stop.load()) {
      int eff_interval = ctx->cfg.rtt.interval_ms.load(); // 每一轮探测开始时主动 pull 最新参数！
      ... 按照 5000ms 执行探测与 100ms 步长分段休眠
  }
```

#### 2. 时间单位解析状态机：`parseDurationMs`（`server/src/weaknet_config.cpp:111`）
- **支持单位**：`ms`（毫秒）、`s`（秒）、`m`（分钟）、`h`（小时），纯数字默认视为 `ms`。
- **解析算法与防溢出保护**：
  ```cpp
  bool parseDurationMs(const std::string& v, uint32_t* out) {
      // 1. 剥离前后空白字符
      // 2. 扫描数字部分与单位字符（如 "5000" 与 "ms"）
      // 3. 乘法溢出校验：乘以倍率（s=1000, m=60000）前先与 UINT32_MAX 比较，防止整型回绕溢出
      // 4. 非法格式（如 "abc"、"5x"）直接返回 false
  }
  ```

#### 3. 点分键名路由：`splitMonitorKey`（`server/src/weaknet_config.cpp:366`）
- 用户输入键名为 `"rtt.interval"`，函数提取第一个圆点，分割为两段：
  - `monitor = "rtt"`（定位对应的监控插件）；
  - `field = "interval"`（定位监控器内部具体的属性名）。
- **健壮性保障**：若无圆点、多个圆点或键名为空，直接阻断并返回语法错误。

#### 4. 完整的 CLI 错误反馈技术闭环：
```
终端执行非法命令：weaknet-cli set rtt.interval 100h
   │
   ▼ client/client.cpp:883 (发送 D-Bus 请求)
   │
   ▼ server/src/dbus_service.cpp:512 (调用 setMonitorParam)
   │   └─ parseDurationMs 解析出 360,000,000 ms
   │   └─ checkRange(ms, 100, 600000) 校验失败！
   │   └─ 填充错误信息: *error = "rtt.interval: must be 100ms~600000ms"
   │   └─ 返回 false
   │
   ▼ server/src/dbus_service.cpp:525
       dbus_message_new_error(msg, "com.example.WeakNet.Error.InvalidParam", error.c_str())
       回复客户端明确的 D-Bus 错误帧
   │
   ▼ client/weaknet_cli.cpp 收到错误
       终端标准错误输出红字: "[ERROR] rtt.interval: must be 100ms~600000ms"
       进程退出码返回 1，服务端内存旧值完全保持不变！
```

#### 5. 动态调参全景清单（调的参数到底是什么？）

##### (1) 传统物理链路与统计监控器参数
| 监控器 | 参数名（点分键） | 默认值 | 允许调节范围 | 物理与业务含义 |
|:---|:---|:---|:---|:---|
| **rtt** (延迟) | `rtt.interval` | `10000ms` (10s) | 100ms ~ 600s | ICMP Ping 发包采样周期。弱网排障时可调快至 500ms 抓取瞬时网络毛刺。 |
| | `rtt.target` | `"223.5.5.5"` | 合法 IP/域名 | 延迟探测目标（可热切为内网网关 IP 或业务服务器地址）。 |
| | `rtt.timeout` | `800ms` | 100ms ~ 60s | 单次 Ping 等待对端回包的超时阈值。 |
| **jitter** (抖动) | `jitter.interval` | `2000ms` (2s) | 100ms ~ 600s | 网络时延抖动计算的采样频次。 |
| | `jitter.window_size` | `30` | 5 ~ 1000 | 计算标准差/抖动时的历史时延样本滑动窗口大小。 |
| **rssi** (Wi-Fi) | `rssi.interval` | `10000ms` (10s) | 1s ~ 600s | 向 `wpa_supplicant` 轮询 Wi-Fi 天线信号强度的周期。 |
| **tcp_loss** (丢包) | `tcp_loss.interval` | `10000ms` (10s) | 1s ~ 600s | 通过 Netlink `SOCK_DIAG` dump 内核 `tcp_info` 计算全局丢包率的周期。 |
| **traffic** (流量) | `traffic.interval` | `10000ms` (10s) | 1s ~ 600s | 流量分析线程计算总吞吐量（Mbps）和抓取 Top 活动流的分析周期。 |
| **quality** (质量) | `quality.interval` | `15000ms` (15s) | 1s ~ 600s | 综合时延、丢包率、RSSI 加权评定网络质量等级（Good/Fair/Poor/Bad）的周期。 |
| **bluetooth** (蓝牙) | `bluetooth.interval` | `3000ms` (3s) | 1s ~ 60s | 通过 BlueZ D-Bus 总线全量刷新蓝牙设备拓扑与连接状态的周期。 |

##### (2) 内核态 eBPF 监控器参数
| 监控器 | 参数名（点分键） | 默认值 | 允许调节范围 | 物理与业务含义 |
|:---|:---|:---|:---|:---|
| **dns** (DNS延迟) | `dns.interval` | `10000ms` (10s) | 1s ~ 600s | 用户态排空 Perf Buffer 并计算 DNS 响应平均耗时/超时率的周期。 |
| | `dns.assessment_profile`| `"INTERNET_ACCESS"` | 字符串 | 评估画像模式（`NETWORK_ONLY` 仅内网 / `INTERNET_ACCESS` 访问公网）。 |
| **wifi_loss** (无线丢包) | `wifi_loss.interval` | `10000ms` (10s) | 1s ~ 600s | 从 BPF Map 提取网卡驱动层 `NETDEV_TX_BUSY` 与 `tx_drops` 的周期。 |
| **http_latency** (HTTP) | `http_latency.interval`| `10000ms` (10s) | 1s ~ 600s | 提取 HTTP TTFB 首字节响应耗时与状态码分布的计算周期。 |
| **process_profiler** | `process_profiler.interval`| `15000ms` (15s) | 1s ~ 600s | 汇总各进程消耗带宽与 TCP 重传次数的网络画像周期。 |
| **tcp_retrans** | `tcp_retrans.interval` | `15000ms` (15s) | 1s ~ 600s | 提取每条 TCP 4 元组连接级重传率的统计周期。 |
| **tcp_conn** | `tcp_conn.interval` | `15000ms` (15s) | 1s ~ 600s | 汇总入向连接建立耗时分布与生命周期直方图的周期。 |
| **skb_drop** | `skb_drop.interval` | `10000ms` (10s) | 1s ~ 600s | 统计内核协议栈丢包原因直方图（`enum skb_drop_reason`）的拉取周期。 |
| **所有 eBPF 监控器** | `<name>.bpf_obj` | `"build/*.bpf.o"` | 文件路径 | **支持动态热切换底层 eBPF 字节码目标文件**，方便在实验与排障时无缝热替换探针。 |

##### (3) 主动探测引擎参数（Active Probe / Captive Portal）
| 参数名（点分键） | 默认值 | 允许调节范围 | 物理与业务含义 |
|:---|:---|:---|:---|
| `active_probe.enabled` | `false` | `true / false` | 受控主动连通性探测的全局总开关。 |
| `active_probe.interval` | `30000ms` (30s) | 5s ~ 3600s | 发起主动 HTTPS 连通性探测与强制门户（Captive Portal）认证检测的周期。 |
| `active_probe.timeout` | `3000ms` (3s) | 500ms ~ 30s | 单次 HTTP/HTTPS 探测建连与请求的超时时间。 |
| `active_probe.targets` | `""` (默认空) | 逗号分隔字符串 | 探测目标配置（格式为 `"id\|host\|port"`，如 `"ali\|223.5.5.5\|443,qq\|119.29.29.29\|443"`）。 |

##### (4) 运行时单监控器启停管控命令（无需重启整个服务）
- `weaknet-cli disable <monitor>`：将 `cfg.<monitor>.enabled` 设为 `false`，并触发 `ctx-><monitor>_stop.store(true)`，毫秒级安全终止该 Worker 线程。
- `weaknet-cli enable <monitor>`：重新拉起该 Worker 工作线程。
- `weaknet-cli restart <monitor>`：以最新参数冷启动重启指定监控器。

---

### 7.2 【对应第 2 点】8 个 eBPF 程序的内核函数签名与寄存器映射表

在 Linux 内核中，eBPF 程序通过 `struct pt_regs *ctx` 参数挂载在内核函数的入口（kprobe）或返回点（kretprobe）。下面列出 8 个程序挂载的真实内核函数签名，以及代码中如何精准提取参数：

| eBPF 文件 | 挂载点类型与内核函数 | 真实内核函数签名 | eBPF 提取参数的寄存器映射 |
|:---|:---|:---|:---|
| `flow_rate.bpf.c` | `kprobe/ip_queue_xmit` | `int ip_queue_xmit(struct sock *sk, struct sk_buff *skb, struct flowi *fl, int flags)` | `PARM1` $\to$ `struct sock*`<br>`PARM2` $\to$ `struct sk_buff*` (从中读 `len`) |
| `flow_rate.bpf.c` | `kprobe/udp_sendmsg` | `int udp_sendmsg(struct sock *sk, struct msghdr *msg, size_t len)` | `PARM1` $\to$ `struct sock*`<br>`PARM3` $\to$ `len` (本次发送字节数) |
| `tcp_retransmit.bpf.c` | `kprobe/tcp_retransmit_skb` | `int tcp_retransmit_skb(struct sock *sk, struct sk_buff *skb, int segs)` | `PARM1` $\to$ `struct sock*` (从中提取 4 元组) |
| `tcp_retransmit.bpf.c` | `kprobe/tcp_sendmsg` | `int tcp_sendmsg(struct sock *sk, struct msghdr *msg, size_t size)` | `PARM1` $\to$ `struct sock*`<br>`PARM3` $\to$ `size` (用于 MSS=1460 估算发送段数) |
| `dns_monitor.bpf.c` | `kprobe/udp_sendmsg`<br>`kprobe/udp_recvmsg` | `int udp_sendmsg(struct sock *sk, ...)`<br>`int udp_recvmsg(struct sock *sk, ...)` | `PARM1` $\to$ `struct sock*` (匹配目的/源端口为 53 的 DNS 包) |
| `wifi_packet_loss.bpf.c` | `tracepoint/net/net_dev_xmit` | 内核预置 tracepoint：`TP_PROTO(struct sk_buff *skb, int rc, ...)` | **固定偏移直读**：<br>`(void*)ctx + 8` $\to$ `skb*`<br>`(void*)ctx + 20` $\to$ `rc` (返回码) |
| `http_latency.bpf.c` | `kprobe/tcp_sendmsg` | `int tcp_sendmsg(struct sock *sk, struct msghdr *msg, size_t size)` | `PARM1` $\to$ `struct sock*`<br>`PARM2` $\to$ `struct msghdr*` (解引用 iov 读 HTTP 明文) |
| `http_latency.bpf.c` | `kretprobe/tcp_recvmsg_locked` | `int tcp_recvmsg_locked(struct sock *sk, ...)` | `PARM1(ret)` $\to$ 函数返回值 `ret` (实际接收的字节数) |
| `tcp_conn_stats.bpf.c` | `kretprobe/inet_csk_accept` | `struct sock *inet_csk_accept(struct sock *sk, ...)` | `PARM1(newsk)` $\to$ 返回值，指向新建立的 `struct sock*` (含 ERR_PTR 过滤) |
| `skb_drop.bpf.c` | `tracepoint/skb/kfree_skb` | 内核预置 tracepoint：`TP_PROTO(struct sk_buff *skb, void *location, enum skb_drop_reason reason)` | 直接读取结构体成员：<br>`ctx->reason` (丢包原因枚举)<br>`ctx->protocol` (协议号) |
| `a2dp_media.bpf.c` | `kprobe/l2cap_sock_sendmsg` | `int l2cap_sock_sendmsg(struct socket *sock, struct msghdr *msg, size_t len)` | `PARM1` $\to$ `struct socket*` (从中读 `sk->sk_user_data` $\to$ `l2cap_chan` $\to$ BDADDR) |

---

### 7.3 【对应第 4 点】Wi-Fi 与蓝牙 2.4GHz 频段冲突（Coexistence RCA）根因分析

#### 1. 物理学与射频背景
- **同频干扰**：Wi-Fi 802.11 b/g/n 协议工作在 2.4GHz ISM 频段（2412MHz ~ 2472MHz，覆盖 13 个信道，信道带宽 20MHz/40MHz）。
- 经典蓝牙（Bluetooth BR/EDR）和低功耗蓝牙（BLE）同样工作在 2.4GHz（2402MHz ~ 2480MHz，划分为 79 个 1MHz 信道进行跳频传输）。
- 当嵌入式设备（如 Radxa Cubie A7A 开发板）**同时开启 Wi-Fi 传输和蓝牙 A2DP 高清音频播放**时，蓝牙跳频脉冲会直接切入 Wi-Fi 信道频带内，造成射频信噪比骤降，表现为**双方 RSSI 异常联动下跌，并伴随丢包率激增**。

#### 2. 本项目的冲突判据算法（`server/src/band_conflict_detector.cpp`）
该模块不依赖黑盒机器学习，采用严格的**数理统计与信号处理三层校验**：

```
样本队列校验 (历史滑动窗口 ≥ 10 个有效样本)
       │
       ▼
【第 1 层：基线偏差判据 (Drop Threshold)】
取过去 20 个样本均值作为无干扰基线：wifiBaseline, btBaseline；
取最近 3 个样本均值作为当前值：wifiCurrent, btCurrent；
双方跌幅：wifiDrop = wifiBaseline - wifiCurrent，btDrop = btBaseline - btCurrent；
门限要求：双方跌幅必须【同时 ≥ 6 dBm】，排除普通信号轻微波动。
       │
       ▼
【第 2 层：皮尔逊积矩相关系数校验 (Pearson Correlation)】
计算 Wi-Fi 序列与蓝牙序列在时间轴上的相关系数 r：
       r = Σ(x - x̄)(y - ȳ) / [ sqrt(Σ(x - x̄)²) * sqrt(Σ(y - ȳ)²) ]
判定：若为独立外部衰减（如人走开），两者波动无强相关；
若为同频干扰，双方必同步下跌，要求 r 显著为负相关：r ≤ -0.4。
       │
       ▼
【第 3 层：综合置信度评分 (Confidence Score)】
降幅贡献权重 50%：dropScore = min(min(wifiDrop, btDrop), 20) / 20 * 50
相关性贡献权重 50%：corrScore = |r| * 50
总置信度：confidence = dropScore + corrScore (满分 100)
       │
       ▼
【输出结论门槛】
仅当 confidence ≥ 50 分时，输出权威根因诊断结论：
向 D-Bus 派发携带 "[band_conflict]" 前缀的告警事件，
向用户态 Web 前端给出针对性修复建议：“建议将 Wi-Fi 切换至 5GHz 频段，或将 2.4G 信道避让至信道 1/6/11”。
```

---

### 7.4 【对应第 5 点】OpenSpec 规划冻结哈希（Fingerprint）与密码学防篡改全流程

在敏捷开发和 AI 辅助开发中，最大的工程风险是“需求蔓延”与“实现期倒改设计”。本项目通过基于 SHA-256 的多重指纹链，在技术层面实现了防伪与防篡改：

#### 1. 什么是规划基线冻结？（`snapshot_update.sh --freeze-planning-baseline`）
在规划阶段（Planner）完成 `proposal.md`、`design.md`、`specs/` 和 `tasks.md` 后，必须执行冻结命令。系统通过 Node.js 脚本针对以下三部分分别计算密码学哈希并记录在 `ai_snapshot.json` 中：
1. **`planned_base_specs_fingerprint`**：全仓库主规格文件的 SHA-256 聚合哈希；
2. **`planned_change_fingerprint`**：当前变更目录下 `proposal.md`、`design.md`、`tasks.md` 内容的标准规范哈希（Canonical JSON SHA-256）；
3. **`planned_tdd_policy_sha256`**：当前变更中批准的 TDD 策略块（测试例外与适用性策略）哈希；
4. **`planned_integration_completeness_sha256`**：`design.md` 中唯一的 `Integration Completeness v1` 表格块的专属 SHA-256 哈希。

#### 2. 实现基线锁定（`--freeze-implementation-base`）
- 在修改第一行生产代码前，执行锁定命令，记录当前的完整 Git Commit ID：`implementation_base_commit`。
- **作用**：确保后续所有的变更足迹（Footprint）计算、代码 Diff 审计都基于**修改前的这一精确提交节点**，严禁“改完代码后再冻结基线”（若先改代码再冻结，Diff 为空，足迹归零，会被 Evaluator 判定为无效变更直接打回）。

#### 3. 执行期的实时熔断机制（Run-time Integrity Guard）
每次 Generator 运行 `task_verify.sh` 或 Evaluator 运行 `evaluator_check.sh` 时，系统底层均通过 `integration_surface_lib.js` 重新计算当前的 `design.md` 指纹：
```javascript
// scripts/integration_surface_lib.js
if (snapshot.planned_change_fingerprint !== planning.planning_fingerprint ||
    snapshot.planned_tdd_policy_sha256 !== planning.tdd_policy_sha256 ||
    snapshot.planned_integration_completeness_sha256 !== plan.block_sha256) {
    throw Error('approved Integration planning baseline is missing or stale');
}
```
- **熔断后果**：如果开发者或 AI 在实现代码途中**改动了 `design.md` 里的任何一个标点符号**，当前计算出的哈希与冻结快照不符，Harness 会立即报错拒绝记录证据，所有已跑完的任务证据瞬间作废！开发者必须彻底废弃当前分支并重新规划。
- **防伪价值**：杜绝了任何“边做边改需求”、“伪造测试通过”、“事后篡改设计”的可能，保证代码与规划的一致性。

