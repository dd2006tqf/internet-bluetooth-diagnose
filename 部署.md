# WEAK_NET 项目交叉编译与开发板部署指南

> 目标：在 x86 主机上用 Docker + QEMU 模拟 ARM64 环境编译项目，把产物部署到 Radxa Cubie A7A（aarch64 / Debian 11 / 内核 5.15.147-99btf1-a733）运行。
>
> 本文档已结合项目源码逐项核对，所有路径、行号、Makefile 目标均为实际值。

## 整体流程

```
A7A 开发板导出 vmlinux.h（来自 5.15.147-99btf1-a733 内核 BTF）
          ↓
电脑上的 ARM64 Debian 11 容器（Docker + QEMU 模拟）
          ↓
编译 ARM64 C++ 程序 + eBPF 对象（clang -target bpf）
          ↓
打包 weaknet-aarch64.tar.gz
          ↓
scp 到 A7A
          ↓
开发板运行验证（root + dbus-run-session）
```

## 项目核对清单（已验证）

在动手前，先明确项目实际依赖与构建规则，避免踩坑：

| 核对项 | 实际情况 | 影响 |
|--------|----------|------|
| 服务端链接库 | `pkg-config --libs dbus-1` + `-lglog` + `-lbpf`（[server/Makefile:4](file:///home/tanqf/AI-powered-Network-Diagnostics/server/Makefile)） | 容器需装 dbus/glog/libbpf，**不需要** libnl/libcap |
| 顶层 `all` 目标 | `all: dirs server-client-lib`，只构建客户端（[Makefile:13-15](file:///home/tanqf/AI-powered-Network-Diagnostics/Makefile)） | 服务端必须单独 `make server-bin` |
| vmlinux.h 规则 | `server/build/vmlinux.h` 已存在则跳过生成（[server/Makefile:21-30](file:///home/tanqf/AI-powered-Network-Diagnostics/server/Makefile)） | `make clean` 会删 `server/build`，需重新放 vmlinux.h |
| eBPF 架构宏 | `flow_rate.bpf.c` 第 1 行、`a2dp_media.bpf.c` 第 **11** 行硬编码 `__TARGET_ARCH_x86` | 必须改为 `__TARGET_ARCH_arm64` |
| eBPF 对象路径 | traffic_analyzer 用 `../build/flow_rate.bpf.o`（[traffic_analyzer.cpp:30](file:///home/tanqf/AI-powered-Network-Diagnostics/server/src/traffic_analyzer.cpp)）；bt_monitor 用 `build/a2dp_media.bpf.o`（[bt_monitor.cpp:1083](file:///home/tanqf/AI-powered-Network-Diagnostics/server/src/bt_monitor.cpp)） | 路径不一致，需统一为 `build/` 前缀，从 `server/` 目录启动 |
| libbpf 版本 | 项目用 CO-RE（`bpf_core_read`、LRU_HASH），Debian 11 自带 0.3 太旧 | 容器内需源码编译 libbpf 1.3.0 |
| 项目是否用 libnl | 否（grep 无引用） | 容器无需装 libnl |

---

## 一、确认电脑能运行 ARM64 容器

先注册 QEMU 模拟器（首次执行，需联网拉镜像）：

```bash
docker run --rm --privileged multiarch/qemu-user-static --reset -p yes
```

> 若该镜像拉取慢，可改用 Docker 官方推荐方式：
> ```bash
> docker run --privileged --rm tonistiigi/binfmt --install arm64
> ```

验证 ARM64 容器可运行：

```bash
docker run --rm --platform linux/arm64 debian:11 uname -m
```

**期望输出**：

```
aarch64
```

只要得到 `aarch64`，后面就不用再执行任何 QEMU 注册命令。如果出现 `exec format error`，用上面的 `tonistiigi/binfmt` 方式重试。

---

## 二、从开发板取得精确的 vmlinux.h

vmlinux.h 必须来自开发板**当前运行内核**（`5.15.147-99btf1-a733`）的 BTF，不能用电脑 x86 内核的，否则 eBPF 加载时会 BTF 类型不匹配。

**开发板执行**：

```bash
bpftool btf dump file /sys/kernel/btf/vmlinux format c > ~/vmlinux.h
wc -l ~/vmlinux.h
```

**期望**：约 2.4M，11 万行以上。

**电脑执行**（IP 按实际改）：

```bash
cd ~/AI-powered-Network-Diagnostics
mkdir -p board-assets
scp radxa@192.168.2.77:/home/radxa/vmlinux.h board-assets/vmlinux.h

# 验证
ls -lh board-assets/vmlinux.h
wc -l board-assets/vmlinux.h
```

---

## 三、修改 eBPF 源文件的架构宏

两个 eBPF 源文件当前硬编码为 x86，目标开发板是 ARM64，必须改：

- [server/src/flow_rate.bpf.c:1](file:///home/tanqf/AI-powered-Network-Diagnostics/server/src/flow_rate.bpf.c) — 第 1 行
- [server/src/bpf/a2dp_media.bpf.c:11](file:///home/tanqf/AI-powered-Network-Diagnostics/server/src/bpf/a2dp_media.bpf.c) — 第 11 行

**电脑执行**：

```bash
cd ~/AI-powered-Network-Diagnostics

sed -i.bak \
  's/^#define __TARGET_ARCH_x86$/#define __TARGET_ARCH_arm64/' \
  server/src/flow_rate.bpf.c \
  server/src/bpf/a2dp_media.bpf.c

grep -n '__TARGET_ARCH' \
  server/src/flow_rate.bpf.c \
  server/src/bpf/a2dp_media.bpf.c
```

**期望输出**：

```
server/src/flow_rate.bpf.c:1:#define __TARGET_ARCH_arm64
server/src/bpf/a2dp_media.bpf.c:11:#define __TARGET_ARCH_arm64
```

> 该宏影响 `PT_REGS_PARM*` 等寄存器参数解析，改错会导致 eBPF 程序在 ARM64 上读不到正确的入参。

---

## 四、统一 eBPF 对象文件运行路径

项目存在路径不一致：

- 流量分析器用 `"../build/flow_rate.bpf.o"`（[traffic_analyzer.cpp:30](file:///home/tanqf/AI-powered-Network-Diagnostics/server/src/traffic_analyzer.cpp)）
- 蓝牙分析器用 `"build/a2dp_media.bpf.o"`（[bt_monitor.cpp:1083](file:///home/tanqf/AI-powered-Network-Diagnostics/server/src/bt_monitor.cpp)）

两者无法在同一个工作目录下同时正确解析。统一为从 `server/` 目录启动、用 `build/` 前缀：

```bash
cd ~/AI-powered-Network-Diagnostics

sed -i.bak \
  's#"\.\./build/flow_rate\.bpf\.o"#"build/flow_rate.bpf.o"#' \
  server/src/traffic_analyzer.cpp

grep -n 'setBpfObjectPath' server/src/traffic_analyzer.cpp
```

**期望输出**：

```
    analyzer_->setBpfObjectPath("build/flow_rate.bpf.o");
```

> 以后在开发板运行时必须 `cd ~/weaknet/server` 再启动服务端，保证两个 `.bpf.o` 路径都能正确解析。

---

## 五、创建可重复使用的 ARM64 构建镜像

在电脑项目目录创建 Dockerfile：

```bash
cd ~/AI-powered-Network-Diagnostics
mkdir -p .docker/arm64-builder
```

写入 `.docker/arm64-builder/Dockerfile`：

```dockerfile
FROM arm64v8/debian:11

ARG DEBIAN_FRONTEND=noninteractive
ARG LIBBPF_VERSION=v1.3.0

# 基础编译依赖（与项目 LDFLAGS 一致：dbus-1 + glog + bpf）
RUN apt-get update && \
    apt-get install -y --no-install-recommends \
        build-essential \
        g++ \
        make \
        pkg-config \
        clang \
        llvm \
        git \
        ca-certificates \
        file \
        tar \
        gzip \
        libdbus-1-dev \
        libgoogle-glog-dev \
        libelf-dev \
        zlib1g-dev \
        libcap-dev && \
    rm -rf /var/lib/apt/lists/*

# 从源码编译 libbpf（Debian 11 自带 0.3 太旧，项目 CO-RE 需要 1.0+）
RUN git clone \
        --depth 1 \
        --branch "${LIBBPF_VERSION}" \
        https://github.com/libbpf/libbpf.git \
        /tmp/libbpf && \
    make -C /tmp/libbpf/src -j"$(nproc)" && \
    make -C /tmp/libbpf/src install \
        PREFIX=/usr/local \
        LIBDIR=/usr/local/lib && \
    echo '/usr/local/lib' > /etc/ld.so.conf.d/libbpf-local.conf && \
    ldconfig && \
    rm -rf /tmp/libbpf

ENV CPATH=/usr/local/include
ENV CPLUS_INCLUDE_PATH=/usr/local/include
ENV LIBRARY_PATH=/usr/local/lib
ENV LD_LIBRARY_PATH=/usr/local/lib
ENV PKG_CONFIG_PATH=/usr/local/lib/pkgconfig

WORKDIR /src
```

构建 ARM64 编译镜像：

```bash
docker build \
  --platform linux/arm64 \
  -t weaknet-builder:bullseye-arm64 \
  .docker/arm64-builder
```

首次构建约 5-10 分钟（要编 libbpf）。构建一次后可复用。

---

## 六、在 ARM64 容器里编译整个项目

执行下面整段（一次性完成清理、放 vmlinux.h、编译、打包）：

```bash
cd ~/AI-powered-Network-Diagnostics

docker run --rm \
  --platform linux/arm64 \
  -v "$PWD":/src \
  -w /src \
  -e HOST_UID="$(id -u)" \
  -e HOST_GID="$(id -g)" \
  weaknet-builder:bullseye-arm64 \
  bash -lc '
set -euxo pipefail

# 清除之前其他架构留下的产物
make clean

# clean 会删除 server/build，所以这里再放入板端 vmlinux.h
mkdir -p server/build
cp board-assets/vmlinux.h server/build/vmlinux.h

# 顶层 all 只构建客户端，服务端需单独构建
make server-bin -j"$(nproc)"
make client-lib -j"$(nproc)"

echo "========== 架构验证 =========="
file \
  server/bin/weaknet-dbus-server \
  client/bin/test-client \
  client/lib/libweaknet.so \
  server/build/flow_rate.bpf.o \
  server/build/a2dp_media.bpf.o

echo "========== 动态库依赖验证 =========="
ldd server/bin/weaknet-dbus-server

# 准备部署包
rm -rf dist-arm64
mkdir -p \
  dist-arm64/server/bin \
  dist-arm64/server/build \
  dist-arm64/server/logs/server \
  dist-arm64/client/bin \
  dist-arm64/client/lib \
  dist-arm64/lib

cp server/bin/weaknet-dbus-server   dist-arm64/server/bin/
cp server/build/flow_rate.bpf.o \
   server/build/a2dp_media.bpf.o    dist-arm64/server/build/
cp client/bin/test-client           dist-arm64/client/bin/
cp client/lib/libweaknet.so         dist-arm64/client/lib/

# 随部署包携带构建时使用的 libbpf，避免运行时版本偏差
cp -a /usr/local/lib/libbpf.so*     dist-arm64/lib/

tar -C dist-arm64 -czf weaknet-aarch64.tar.gz .

chown -R "$HOST_UID:$HOST_GID" dist-arm64 weaknet-aarch64.tar.gz
'
```

---

## 七、验证电脑上的编译结果

```bash
cd ~/AI-powered-Network-Diagnostics

file \
  dist-arm64/server/bin/weaknet-dbus-server \
  dist-arm64/client/bin/test-client \
  dist-arm64/client/lib/libweaknet.so \
  dist-arm64/server/build/flow_rate.bpf.o \
  dist-arm64/server/build/a2dp_media.bpf.o

ls -lh weaknet-aarch64.tar.gz
```

**正确结果**：

| 文件 | 类型 |
|------|------|
| `weaknet-dbus-server` | ELF 64-bit LSB pie executable, ARM aarch64 |
| `test-client` | ELF 64-bit LSB pie executable, ARM aarch64 |
| `libweaknet.so` | ELF 64-bit LSB shared object, ARM aarch64 |
| `flow_rate.bpf.o` / `a2dp_media.bpf.o` | ELF 64-bit LSB relocatable, eBPF |

---

## 八、上传到开发板

```bash
scp \
  ~/AI-powered-Network-Diagnostics/weaknet-aarch64.tar.gz \
  radxa@192.168.2.77:/tmp/
```

---

## 九、开发板部署

**开发板执行**：

```bash
rm -rf ~/weaknet
mkdir -p ~/weaknet
tar -xzf /tmp/weaknet-aarch64.tar.gz -C ~/weaknet

# 检查架构
file \
  ~/weaknet/server/bin/weaknet-dbus-server \
  ~/weaknet/client/bin/test-client \
  ~/weaknet/server/build/flow_rate.bpf.o

# 检查动态库依赖，不应出现 not found
LD_LIBRARY_PATH="$HOME/weaknet/lib:$HOME/weaknet/client/lib:/usr/local/lib" \
ldd "$HOME/weaknet/server/bin/weaknet-dbus-server"
```

> 若开发板缺 libglog，补装：`sudo apt-get install -y libglog-dev`。
> libbpf 已随包携带在 `~/weaknet/lib/`，无需再装。

---

## 十、在开发板启动服务并测试

项目服务端使用 Session Bus，而蓝牙监控连接 System Bus；服务端和测试客户端要处于同一个 Session Bus。服务端加载 BPF 程序还需要 root 权限。

**开发板执行**：

```bash
sudo env \
  HOME=/home/radxa \
  LD_LIBRARY_PATH="/home/radxa/weaknet/lib:/home/radxa/weaknet/client/lib:/usr/local/lib" \
  dbus-run-session -- bash -lc '
set -eux
cd /home/radxa/weaknet/server
mkdir -p logs/server

./bin/weaknet-dbus-server >/tmp/weaknet-server.log 2>&1 &
SERVER_PID=$!
sleep 8

cd /home/radxa/weaknet
./client/bin/test-client get
./client/bin/test-client health

kill "$SERVER_PID" 2>/dev/null || true
wait "$SERVER_PID" 2>/dev/null || true
'
```

**查看日志**：

```bash
sudo tail -n 200 /tmp/weaknet-server.log
```

**检查 BPF 是否加载成功**：

```bash
sudo bpftool prog show
sudo bpftool map show
```

能看到 `flow_rate` 相关的 program / map 即说明 eBPF 加载成功。

---

## 附录：常见问题速查

| 现象 | 原因 | 解决 |
|------|------|------|
| `exec format error` | QEMU 未注册 | 第一部的 `tonistiigi/binfmt --install arm64` |
| `vmlinux.h not found` | vmlinux.h 没放对位置 | 确认 `server/build/vmlinux.h` 存在（`make clean` 后要重新放） |
| `bpf_core_read` 未声明 | 容器内 libbpf 太旧 | 重新 build 镜像（Dockerfile 已用 v1.3.0 源码编译） |
| eBPF 加载失败 `BTF mismatch` | vmlinux.h 与运行内核不匹配 | 重新从开发板当前内核生成 vmlinux.h |
| 运行时 `libbpf.so.1: cannot open` | 开发板缺 libbpf 1.x | 用部署包里的 `~/weaknet/lib/libbpf.so*`，设好 `LD_LIBRARY_PATH` |
| 运行时 `libglog.so` 找不到 | 开发板缺 glog | `sudo apt-get install -y libglog-dev` |
| 服务端找不到 `.bpf.o` | 工作目录不对 | 必须 `cd ~/weaknet/server` 再启动 |
| `PT_REGS_PARM` 读到错值 | eBPF 架构宏没改 | 第三步：改为 `__TARGET_ARCH_arm64` |
| 容器内 `make server-bin` 找不到目标 | 用错了 Makefile | 顶层 `Makefile` 有 `server-bin` 目标，直接 `make server-bin` 即可（它会 `make -C server`） |

---

## 修改回滚

第三、四步的 `sed` 都会生成 `.bak` 备份。若要回滚到 x86 版本：

```bash
cd ~/AI-powered-Network-Diagnostics
mv server/src/flow_rate.bpf.c.bak server/src/flow_rate.bpf.c
mv server/src/bpf/a2dp_media.bpf.c.bak server/src/bpf/a2dp_media.bpf.c
mv server/src/traffic_analyzer.cpp.bak server/src/traffic_analyzer.cpp
```
