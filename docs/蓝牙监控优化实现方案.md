# 蓝牙监控优化 — 最终实施方案与实施计划

> **文档版本**：v2.0  
> **编制日期**：2026-07-24  
> **文档状态**：已定稿，待执行  
> **适用项目**：AI-powered-Network-Diagnostics / WEAK_NET Server  

---

## 目录

- [1. 项目背景](#1-项目背景)
- [2. 目标与范围](#2-目标与范围)
- [3. 技术路线选型](#3-技术路线选型)
- [4. 详细技术实现方案](#4-详细技术实现方案)
  - [4.1 总体架构](#41-总体架构)
  - [4.2 模块一：频段冲突检测器](#42-模块一频段冲突检测器)
  - [4.3 模块二：A2DP 音频质量监控](#43-模块二a2dp-音频质量监控)
  - [4.4 模块三：设备距离估算](#44-模块三设备距离估算)
  - [4.5 模块四：eBPF 蓝牙流量采集](#45-模块四ebpf-蓝牙流量采集)
  - [4.6 模块五：D-Bus/eBPF 融合层](#46-模块五d-busebpf-融合层)
  - [4.7 数据流程设计](#47-数据流程设计)
  - [4.8 线程调度与集成](#48-线程调度与集成)
  - [4.9 NetInfo 扩展](#49-netinfo-扩展)
  - [4.10 事件集成](#410-事件集成)
- [5. 资源需求](#5-资源需求)
- [6. 分阶段实施计划](#6-分阶段实施计划)
- [7. 风险管理策略](#7-风险管理策略)
- [8. 质量保障措施](#8-质量保障措施)
- [9. 验收标准](#9-验收标准)
- [10. 项目时间线](#10-项目时间线)

---

## 1. 项目背景

### 1.1 项目概述

本项目为 `WEAK_NET Server`（AI-powered-Network-Diagnostics）的蓝牙监控子系统优化工程。当前蓝牙监控线程已实现对 BlueZ 适配器状态、设备发现、RSSI 信号强度、连接/配对状态的基础监控，但存在以下关键能力缺失：

| 不足项 | 影响 | 优先级 |
| ------ | ---- | ------ |
| 不监控蓝牙音频质量 | 无法发现"蓝牙卡顿"问题 | 中 |
| 无法检测 Wi-Fi/蓝牙频段冲突 | 漏报"2.4GHz 干扰"场景 | 高 |
| 无法估算设备距离 | 缺乏空间感知能力 | 低 |
| 无音频编解码器信息 | 无法判断音频带宽限制 | 低 |

### 1.2 现有能力基线

| 能力 | 实现方式 | 代码位置 |
| ---- | -------- | -------- |
| 适配器状态监控 | BlueZ D-Bus Adapter1 接口 | `refreshAdapterState()` |
| 设备发现与管理 | Device1 接口属性解析 | `parseDeviceProperties()` |
| RSSI 信号强度 | Device1.RSSI 属性 | `getDeviceRssi()` |
| RSSI 历史趋势 | `rssiHistory`（最多 30 次） | `bt_monitor.hpp` L57 |
| 连接/配对状态 | Device1.Connected/Paired | `parseDeviceProperties()` |
| 事件回调机制 | `emitBluetoothDeviceChanged` | `start_bt_monitor_thread()` |
| 信号等级评估 | excellent/good/fair/poor/very_poor | `rssiLevel()` |

### 1.3 现有架构约束

- BtMonitor 单例运行在独立蓝牙监测线程中，3 秒刷新周期
- 网络质量评估运行在 `network_quality_thread` 中，15 秒刷新周期
- 所有事件通过 `EventManager` 统一路由，经 D-Bus 信号对外暴露
- 项目已有 eBPF 基建（`flow_rate.bpf.c` + `NetTrafficAnalyzer`），采用 libbpf + CO-RE + vmlinux.h 模式，已建立降级模式

---

## 2. 目标与范围

### 2.1 优化目标

| 编号 | 目标 | 描述 | 优先级 |
| ---- | ---- | ---- | ------ |
| G1 | 频段冲突检测 | 关联 Wi-Fi RSSI 与蓝牙 RSSI，识别 2.4GHz 频段冲突 | P0 |
| G2 | 音频质量监控 | 实时监控 A2DP 音频流的丢包与延迟，输出音频质量评分 | P1 |
| G3 | 设备距离估算 | 基于 RSSI 路径损耗模型估算设备距离 | P2 |

### 2.2 价值主张

- **提升故障定位能力**：当前只能发现"信号弱"，优化后能定位"是干扰还是距离远"
- **扩展应用场景**：支持智能家居设备布局优化、车载蓝牙诊断
- **增强多协议监控**：补全 Wi-Fi/蓝牙协同分析能力，体现"多协议异构网络"的项目亮点

### 2.3 范围界定

**在范围内：**
- 新建 `band_conflict_detector` 模块，接入 `network_quality_thread`
- 扩展 `BtMonitor` 支持 `org.bluez.MediaTransport1` D-Bus 接口
- 扩展 `BtMonitor` 支持基于 RSSI 路径损耗模型的设备距离估算
- 扩展 `NetInfo` 数据结构，增加蓝牙相关字段
- 新建 eBPF 蓝牙 L2CAP/AVDTP 流量采集程序
- 新建 D-Bus/eBPF 融合层，实现 `effective_active` / `suspected_stall` 判断
- 补充 RAG 知识库诊断条目

**在范围外：**
- LE Audio / BAP 协议支持（第一版仅支持经典蓝牙 A2DP）
- 蓝牙 HFP 免提通话质量监控
- 蓝牙 Mesh 网络监控
- 新增独立线程（严格复用现有线程）

---

## 3. 技术路线选型

### 3.1 路线对比

A2DP 音频质量监控存在两条技术路线，频段冲突检测与设备距离估算两方案公用，不涉及路线分歧。

| 维度 | 路线一：纯 D-Bus 轮询 | 路线二：eBPF + D-Bus 融合 |
| ---- | -------------------- | ------------------------- |
| 数据语义 | BlueZ 上报状态（传输层语义） | 状态 + 内核真实流量（数据路径实证） |
| 能否区分"active 但卡顿" | ❌ | ✅（active + 无流量 = 疑似停滞） |
| 能否区分"暂停 vs 停滞" | ❌ | ✅（短时无流量=暂停，长时=停滞） |
| 延迟精度 | BlueZ Delay 报告值（含空闲期） | 时间加权平均（仅统计有流量窗口） |
| 丢包/发送空洞感知 | ❌ | ✅ |
| 实施成本 | 低（4-5 天） | 高（8-12 天） |
| 内核依赖 | 无 | 内核 5.10+、root、可用蓝牙挂点 |
| ABI 稳定性风险 | 低 | 高（kprobe/tracepoint 非稳定 ABI） |
| 会话映射难度 | 低 | 高（D-Bus 路径 ↔ hci+BDADDR+CID） |
| 性能开销 | 低 | 中（常驻 eBPF + 每秒读 map） |
| 兼容性 | 高（任何 BlueZ 版本） | 中（需挂点探测降级） |
| 降级路径 | 不可用则跳过 | eBPF 不可用自动退化为路线一 |
| 诊断解释力 | 中 | 高（BlueZ 活跃时长/实际流量时长/有效占比/发送空洞） |
| 与现有架构契合度 | 高 | 中（需新增 eBPF 模块，但项目已有基建可复用） |

### 3.2 选型结论

**选定路线二（eBPF + D-Bus 融合）为目标架构，采用分阶段交付策略。**

核心理由：

1. **路线一存在致命短板**：纯 D-Bus 无法区分"正在播放但卡顿"与"正常播放"——这是音频质量监控的核心诉求。BlueZ 的 `State=active` 仅表示传输会话被音频服务获取，不代表声音真在流动。
2. **路线二的降级路径天然覆盖路线一**：eBPF 不可用时自动退化为纯 D-Bus，因此路线二是路线一的超集。
3. **项目已有 eBPF 基建可复用**：`flow_rate.bpf.c` 已建立 libbpf + CO-RE + vmlinux.h 模式，`net_traffic.cpp` 已建立 `bpf_object__open/load/attach` + map 轮询 + 降级模式。
4. **风险可控**：启动期挂点探测（tracepoint→fentry→kprobe→纯 D-Bus）与启动日志明示降级原因，将 ABI 不稳定风险隔离在 eBPF 子模块内。

---

## 4. 详细技术实现方案

### 4.1 总体架构

```
                         ┌──────────────────────────┐
                         │       统计主进程          │
                         │     BtMonitor             │
                         └────────────┬─────────────┘
                                      │
                  ┌───────────────────┼───────────────────┐
                  │                   │                   │
         ┌────────▼────────┐ ┌───────▼────────┐ ┌────────▼────────┐
         │ D-Bus Collector │ │ BandConflict   │ │ eBPF Collector  │
         │ 语义/控制状态层  │ │ Detector       │ │ 内核流量数据层   │
         │ (MediaTransport1)│ │ (Pearson相关)  │ │(a2dp_media.bpf) │
         └────────┬────────┘ └───────┬────────┘ └────────┬────────┘
                  │                  │                    │
   State/Delay/Device/UUID   wifiRssi+btRssi    bytes/packets/gap/send_errors
   Codec/Volume/object path   → 冲突置信度      (BPF Map per device 聚合)
                  │                  │                    │
                  │        ┌─────────▼─────────┐          │
                  └────────►│ BtAudioFusion     │◄─────────┘
                            │ 数据融合与判断    │
                            │ effective/stall   │
                            │ 时间加权 Delay    │
                            └────────┬──────────┘
                                     │
                            ┌────────▼─────────┐
                            │   EventManager   │
                            │ (统一路由)        │
                            └──────────────────┘
```

**三层职责划分：**

| 层 | 职责 | 回答的问题 |
| --- | ---- | ---------- |
| D-Bus 层（语义层） | 通过 `MediaTransport1` 获取会话状态、编解码器、传输层 Delay | "BlueZ 认为当前是什么状态" |
| eBPF 层（数据层） | 挂载内核 L2CAP 发送路径，统计字节/包/间隔/错误 | "内核数据路径实际有没有持续发送数据" |
| 融合层（判断层） | 组合 D-Bus 状态与 eBPF 流量，输出 `effective_active` / `suspected_stall` 等可解释结论 | "当前音频传输是否真正正常" |

### 4.2 模块一：频段冲突检测器

#### 4.2.1 检测原理

```
正常情况：
  Wi-Fi RSSI = -50dBm, 蓝牙 RSSI = -40dBm → 各自正常工作

频段冲突：
  Wi-Fi RSSI 突降至 -70dBm，蓝牙 RSSI 也降至 -70dBm
  → 两者同时下降且降幅相近 → 判定为频段冲突
```

核心判据：**Wi-Fi RSSI 与蓝牙 RSSI 的下降相关性**。

#### 4.2.2 算法步骤

1. **采集历史**：维护 Wi-Fi RSSI 和蓝牙 RSSI 的时间序列（各 30 个样本）
2. **计算基线**：用前 20 个样本计算两者的平均 RSSI 作为基线
3. **检测异常**：当两者同时低于基线 10dBm 以上时，标记为疑似冲突
4. **相关性验证**：计算两者的 Pearson 相关系数，>0.7 确认冲突
5. **置信度输出**：`min(wifiDrop, btDrop)/20 * 50 + correlation*50`

#### 4.2.3 数据来源

- Wi-Fi RSSI：现有 `NetInfo.rssiDbm()`（`net_wifiriss.cpp` 已采集，10s 周期）
- 蓝牙 RSSI：现有 `BtMonitor.getRssiSnapshot()`（3s 周期）

#### 4.2.4 接口设计

```cpp
// band_conflict_detector.hpp
namespace weaknet_dbus {

struct BandConflictResult {
    bool detected = false;
    double confidence = 0.0;       // 0-100%
    double correlation = 0.0;      // Pearson 系数
    int wifiRssiDrop = 0;          // dBm
    int btRssiDrop = 0;            // dBm
    std::string suggestion;
    std::chrono::system_clock::time_point timestamp;
};

class BandConflictDetector {
public:
    void feedSample(int wifiRssi, int btRssi);
    BandConflictResult detect() const;
    static std::string generateSuggestion(const BandConflictResult& r);

private:
    std::deque<int> wifiHistory_;   // MAX=30
    std::deque<int> btHistory_;
    static constexpr size_t MAX_HISTORY = 30;
    static constexpr size_t MIN_SAMPLES = 5;
    static constexpr size_t BASELINE_SAMPLES = 20;
    static constexpr int DROP_THRESHOLD_DB = 10;
    static constexpr double CORRELATION_THRESHOLD = 0.7;

    double baseline(const std::deque<int>& h, size_t n) const;
    double pearson(const std::deque<int>& x, const std::deque<int>& y) const;
};

}  // namespace weaknet_dbus
```

#### 4.2.5 Pearson 相关系数实现

```cpp
double calculatePearson(const std::vector<double>& x,
                        const std::vector<double>& y) {
    if (x.size() != y.size() || x.size() < 5) return 0.0;
    size_t n = x.size();
    double sumX = 0, sumY = 0, sumXY = 0, sumX2 = 0, sumY2 = 0;
    for (size_t i = 0; i < n; ++i) {
        sumX += x[i]; sumY += y[i];
        sumXY += x[i] * y[i];
        sumX2 += x[i] * x[i];
        sumY2 += y[i] * y[i];
    }
    double numerator = n * sumXY - sumX * sumY;
    double denominator = std::sqrt((n * sumX2 - sumX * sumX) *
                                   (n * sumY2 - sumY * sumY));
    if (denominator == 0) return 0.0;
    return numerator / denominator;
}
```

#### 4.2.6 处置建议生成

```cpp
std::string generateConflictSuggestion(const BandConflictResult& r) {
    if (!r.detected) return "";
    if (r.wifiRssiDrop > 20) {
        return "检测到严重 2.4GHz 频段冲突，建议："
               "1) Wi-Fi 切换至 5GHz 频段；"
               "2) 或调整 Wi-Fi 信道至 1/6/11 减少重叠；"
               "3) 蓝牙设备靠近适配器减少发射功率";
    }
    return "检测到轻度 2.4GHz 频段冲突，建议调整设备位置或信道";
}
```

### 4.3 模块二：A2DP 音频质量监控

#### 4.3.1 技术背景

A2DP（Advanced Audio Distribution Profile）是经典蓝牙立体声音频传输协议。BlueZ 通过 `org.bluez.MediaTransport1` 接口暴露音频传输状态。

#### 4.3.2 可获取的指标

| 指标 | D-Bus 属性 | 含义 |
| ---- | ---------- | ---- |
| 状态 | `State` | active/inactive/idle/pending |
| 音频延迟 | `Delay` | 传输延迟（1/10ms 单位） |
| 音量 | `Volume` | 0-127 |
| 编解码器 | `Codec` | SBC(0x00)/AAC/aptX 等 |
| 设备 | `Device` | 关联的设备对象路径 |

#### 4.3.3 D-Bus 采集策略（三层机制）

**第一层：启动时全量快照**
- 调用 `org.freedesktop.DBus.ObjectManager.GetManagedObjects`
- 筛选 `org.bluez.MediaTransport1` 接口的所有对象
- 读取 `Device`、`UUID`、`Codec`、`State`、`Delay`、`Volume`、`Configuration`

**第二层：运行时事件订阅**
- 订阅 `org.freedesktop.DBus.Properties.PropertiesChanged` 信号
- 订阅 `InterfacesAdded` / `InterfacesRemoved` 信号
- 当 `State`、`Delay` 或对象本身变化时，立即更新缓存，无需高频轮询

**第三层：低频校准轮询（兜底）**
- 每 10～30 秒执行一次 `GetAll` 校准
- BlueZ 重启后立即重新获取
- D-Bus 重连后立即重新获取
- 检测到状态矛盾时主动重新获取

**设计原则**：事件驱动为主，低频轮询校准为辅。

#### 4.3.4 数据结构

```cpp
// 蓝牙音频传输状态
struct BtAudioTransport {
    std::string transportPath;      // MediaTransport1 对象路径
    std::string deviceMac;          // 关联设备 MAC
    std::string state;              // active/inactive/idle/pending
    uint16_t delay = 0;             // 音频延迟（1/10ms）
    uint16_t volume = 0;            // 音量 0-127
    uint8_t codec = 0;              // 编解码器 ID (0x00=SBC)
    std::chrono::system_clock::time_point lastActive;
    uint64_t activeDurationMs = 0;  // 累计活跃时长
};

// 蓝牙音频质量评估结果
struct BtAudioQuality {
    std::string deviceMac;
    bool isActive = false;
    double qualityScore = 0.0;      // 0-100
    std::string level;              // excellent/good/fair/poor
    uint16_t currentDelay = 0;
    double activeRatio = 0.0;
    std::vector<std::string> issues;
};
```

#### 4.3.5 质量评分算法

```cpp
double calculateAudioScore(const BtAudioTransport& transport) {
    double score = 100.0;
    // 延迟扣分：>200ms 开始扣分，>500ms 严重卡顿（delay 单位为 1/10ms）
    if (transport.delay > 2000) score -= 40;
    else if (transport.delay > 1000) score -= 20;
    else if (transport.delay > 500) score -= 10;
    // 编解码器质量：SBC 基础扣分
    if (transport.codec == 0x00) score -= 5;
    return std::max(0.0, score);
}
```

#### 4.3.6 BtMonitor 扩展接口

```cpp
class BtMonitor {
public:
    // A2DP 音频监控（D-Bus）
    void refreshAudioTransports();              // 遍历 MediaTransport1（3s 周期）
    bool getAudioQuality(const std::string& mac, BtAudioQuality* out) const;
    bool hasMediaTransportInterface() const;     // 探测降级

private:
    mutable std::mutex audioMutex_;
    std::map<std::string, BtAudioTransport> audioTransports_;

    static constexpr const char* BLUEZ_MEDIA_TRANSPORT_IFACE = "org.bluez.MediaTransport1";
    std::vector<std::string> listMediaTransportPaths(DBusConnection* conn);
    BtAudioTransport parseMediaTransportProperties(DBusConnection* conn,
                                                    const std::string& path);
};
```

### 4.4 模块三：设备距离估算

#### 4.4.1 技术原理

RSSI 与距离的关系遵循**对数距离路径损耗模型**：

```
RSSI(d) = RSSI(d0) - 10 * n * log10(d / d0)
```

- `RSSI(d0)`：参考距离 d0（通常 1 米）处的 RSSI（默认 -59dBm）
- `n`：路径损耗指数（室内默认 2.5）
- `d`：待估算距离

#### 4.4.2 距离估算公式

```cpp
double estimateDistance(int16_t rssiDbm, int16_t txPower = -59) {
    if (rssiDbm == 0) return -1.0;
    const double n = 2.5;
    const double d0 = 1.0;
    double ratio = std::pow(10.0, (txPower - rssiDbm) / (10.0 * n));
    return ratio * d0;
}
```

#### 4.4.3 校准方法

1. 将设备放置在距适配器 1 米处
2. 采集 30 次 RSSI，取平均值作为 `txPower`
3. 将校准值持久化到配置文件
4. 提供 `calibrateDistance(mac, knownDistanceMeters)` 接口

#### 4.4.4 BtDeviceInfo 扩展

```cpp
struct BtDeviceInfo {
    // ... 现有字段 ...
    double estimatedDistance = -1.0;  // 估算距离（米），-1 表示未知
    int16_t calibratedTxPower = -59;  // 校准后的 1 米 RSSI
};
```

### 4.5 模块四：eBPF 蓝牙流量采集

#### 4.5.1 设计目标

eBPF 程序统计数据路径上的行为，但**不直接判断**"用户是否听到声音"或"远端是否收到包"：

| 统计指标 | 说明 |
| -------- | ---- |
| 发送包数量 | 经 L2CAP 发送的音频包计数 |
| 发送字节数 | 累计字节数 |
| 包发送频率 | 每秒包数 |
| 相邻包最大间隔 | 检测发送空洞 |
| 连续无流量时间 | 检测停滞 |
| 发送错误次数 | 内核发送函数返回错误计数 |

#### 4.5.2 eBPF 程序结构

复用 `flow_rate.bpf.c` 的 libbpf + CO-RE + vmlinux.h 模式：

```c
// bpf/a2dp_media.bpf.c
#define __TARGET_ARCH_x86
#include "vmlinux.h"
#include <bpf/bpf_helpers.h>
#include <bpf/bpf_core_read.h>
#include <bpf/bpf_tracing.h>

char LICENSE[] SEC("license") = "GPL";

struct device_key {
    __u32 hci_index;
    __u8  bdaddr[6];
    __u8  direction;   // 0=tx 1=rx
} __attribute__((packed));

struct session_control {
    __u8 dbus_active;
    __u8 enabled;
    __u16 reserved;
};

struct traffic_stats {
    __u64 bytes;
    __u64 packets;
    __u64 send_errors;
    __u64 max_gap_ns;
    __u64 last_packet_ns;
};

// 控制开关 map（用户态写，D-Bus 状态驱动）
struct {
    __uint(type, BPF_MAP_TYPE_HASH);
    __uint(max_entries, 64);
    __type(key, struct device_key);
    __type(value, struct session_control);
} active_sessions SEC(".maps");

// 聚合统计 map（用户态每秒读）
struct {
    __uint(type, BPF_MAP_TYPE_LRU_HASH);
    __uint(max_entries, 64);
    __type(key, struct device_key);
    __type(value, struct traffic_stats);
} bt_stats SEC(".maps");

// 挂点：启动时探测，优先 tracepoint:bluetooth:*，其次 fentry，最后 kprobe
SEC("kprobe/l2cap_stream_send")
int BPF_KPROBE(bt_l2cap_tx, struct l2cap_chan *chan) {
    // 1. 提取 hci_index + bdaddr 组成 key
    // 2. 查 active_sessions，未 enabled 直接 return 0（性能关键）
    // 3. 累加 bytes/packets，更新 max_gap_ns / last_packet_ns
    return 0;
}
```

#### 4.5.3 A2DP 过滤条件纠正

- `PSYNC 0x0019` 写法错误，应为 **AVDTP PSM 0x0019**
- `PSM == 0x0019` 含信令连接 + 媒体连接，不能见 0x0019 即当媒体包
- 需按 `hci_index + BDADDR + L2CAP CID + 方向` 区分
- 第一版无法精确映射到单个 `MediaTransport1` 时，按 `hci_index + BDADDR` 聚合为"设备级指标"，不虚假标为"Transport 级指标"
- LE Audio/BAP 走 ISO 数据路径，第一版仅支持经典蓝牙 A2DP

#### 4.5.4 挂点探测与降级

```cpp
bool BtAudioAnalyzer::start() {
    bpf_object* obj = bpf_object__open(bpfObjPath_.c_str());
    if (!obj) { fallbackReason_ = "open failed"; return false; }
    if (bpf_object__load(obj)) {
        bpf_object__close(obj); fallbackReason_ = "load failed"; return false;
    }
    // 优先级探测：tracepoint > fentry > kprobe > 降级为纯 D-Bus
    if (tryAttachTracepoint())      { hookName_ = "tracepoint:bluetooth:*"; }
    else if (tryAttachFentry())     { hookName_ = "fentry:l2cap_stream_send"; }
    else if (tryAttachKprobe())     { hookName_ = "kprobe:l2cap_stream_send"; }
    else {
        bpf_object__close(obj);
        fallbackReason_ = "no attachable bluetooth hook";
        LOG_INFO(LogModule::BLUETOOTH,
                 "eBPF mode: unavailable, fallback: D-Bus only, reason: "
                 << fallbackReason_);
        return false;  // 降级为纯 D-Bus（路线一）
    }
    mapStatsFd_ = bpf_object__find_map_fd_by_name(obj, "bt_stats");
    mapControlFd_ = bpf_object__find_map_fd_by_name(obj, "active_sessions");
    bpfObj_ = obj; attached_ = true;
    LOG_INFO(LogModule::BLUETOOTH,
             "eBPF mode: enabled, hook: " << hookName_ << ", fallback: none");
    return true;
}
```

**启动日志必须明确记录：**

成功时：
```
eBPF mode: enabled
attached hook: kprobe:l2cap_stream_send
media filter: AVDTP
fallback reason: none
```

失败时：
```
eBPF mode: unavailable
fallback: D-Bus only
reason: target symbol not found
```

#### 4.5.5 BPF Map 控制通道

用户态收到 D-Bus `State=active` 变化后，通过 BPF Map 控制 eBPF 统计开关：

```
D-Bus State = active
        ↓
用户态把 enabled=1 写入 BPF Map
        ↓
eBPF 开始累计详细流量指标

D-Bus State != active
        ↓
用户态清除 enabled=0
        ↓
eBPF 停止详细统计
```

**eBPF 始终加载，通过 BPF Map 控制是否统计，不每次状态变化都卸载/重新加载 eBPF 程序。**

#### 4.5.6 分析器接口

```cpp
class BtAudioAnalyzer {
public:
    bool start();               // 加载 eBPF，探测挂点，失败降级
    void stop();
    bool isAvailable() const;   // eBPF 是否成功 attach

    void setSessionActive(uint32_t hciIndex, const std::string& bdaddr, bool active);
    bool getStats(uint32_t hciIndex, const std::string& bdaddr, BtTrafficStats* out) const;
    std::string attachedHookName() const;

private:
    void* bpfObj_ = nullptr;
    void* link_ = nullptr;
    int mapStatsFd_ = -1;
    int mapControlFd_ = -1;
    bool attached_ = false;
    std::string hookName_;
    std::string fallbackReason_;

    bool tryAttachTracepoint();
    bool tryAttachFentry();
    bool tryAttachKprobe();
};
```

### 4.6 模块五：D-Bus/eBPF 融合层

#### 4.6.1 融合判据

| D-Bus 状态 | eBPF 流量 | 建议解释 |
| ---------- | --------- | -------- |
| `active` | 持续有流量 | 正常音频传输 |
| `active` | 短时间无流量 | 暂停、静音或正常间隙 |
| `active` | 长时间无流量 | 疑似卡顿、传输停滞或状态未及时更新 |
| `idle` | 无流量 | 正常空闲 |
| `idle` | 持续有流量 | 会话映射错误、其他蓝牙流量或 D-Bus 状态不同步 |
| `pending` | 有少量流量 | 建链、协商或等待音频服务获取 |
| 对象已删除 | 仍有流量 | eBPF 会话清理延迟或映射错误 |

```
effective_active = (dbusState == active) && (bytesDuringWindow > minBytes)
suspected_stall  = (dbusState == active) && (now - lastPacketTime > stallThreshold)
```

#### 4.6.2 三种活跃时长

组合后输出三种时长，而非单一"活跃时长"：

| 时长类型 | 计算方式 | 含义 |
| -------- | -------- | ---- |
| `bluez_active_duration` | `State == active` 的累计时间 | BlueZ 传输会话处于 active 状态 |
| `traffic_active_duration` | `bytes_delta > threshold` 的每秒窗口累计 | 实际有数据在发送 |
| `effective_active_duration` | `State == active` 且 eBPF 检测到持续媒体流量 | 真正有效的音频传输 |

最终输出示例：

```
BlueZ活跃时长：60分钟
实际有流量时长：52分钟
有效传输占比：86.7%
active但无流量：8分钟
最长发送空洞：1.3秒
平均BlueZ Delay：185毫秒
```

#### 4.6.3 时间加权 Delay

仅在 `State == active` 且当前统计窗口有实际流量时累计平均延迟，避免空闲期间保持不变的 Delay 值污染统计。

$$\overline{D} = \frac{\sum_i D_i \Delta t_i}{\sum_i \Delta t_i}$$

#### 4.6.4 融合层接口

```cpp
struct BtAudioFusionResult {
    std::string deviceMac;
    bool effectiveActive = false;
    bool suspectedStall = false;
    uint64_t bluezActiveDurationMs = 0;
    uint64_t trafficActiveDurationMs = 0;
    uint64_t effectiveDurationMs = 0;
    double effectiveRatio = 0.0;
    uint64_t maxGapMs = 0;
    double weightedAvgDelayMs = 0.0;
    double qualityScore = 0.0;
    std::string level;
};

class BtAudioFusion {
public:
    BtAudioFusionResult evaluate(const BtAudioTransport& transport,
                                  const BtTrafficStats& stats,
                                  bool ebpfAvailable);
    void setStallThresholds(uint64_t lightMs, uint64_t severeMs);
    void setMinBytesPerWindow(uint64_t bytes);
private:
    uint64_t stallLightMs_ = 300;
    uint64_t stallSevereMs_ = 1000;
    uint64_t minBytesPerWindow_ = 1000;
    std::chrono::system_clock::time_point lastTick_;
    uint64_t lastBytes_ = 0;
};
```

### 4.7 数据流程设计

#### 4.7.1 Phase 1 数据流

```
Wi-Fi RSSI ─────────────┐
(WeakNetMgr.NetInfo)     │
                         ▼
蓝牙 RSSI ──► BandConflictDetector.feedSample()
(BtMonitor       │
 .getRssiSnapshot)▼
              BandConflictDetector.detect()
                         │
                         ▼  (15s, network_quality_thread)
              EventManager.emitNetworkQualityChanged("band_conflict")

BtMonitor.refreshDeviceStates() (3s)
    ├──► parseDeviceProperties → 读取 TxPower
    ├──► estimateDistance(rssi)  → 写入 BtDeviceInfo.estimatedDistance
    └──► refreshAudioTransports() → 遍历 MediaTransport1
              └──► calculateAudioScore → BtAudioQuality
                         │
                         ▼
              EventManager.emitBluetoothDeviceChanged("audio_quality"/"distance")
```

#### 4.7.2 Phase 2 融合数据流

```
D-Bus PropertiesChanged (State=active)
    │
    ▼
BtMonitor.onTransportStateChanged()
    │
    ▼ 写 BPF Map (SessionControl.enabled=1)
BtAudioAnalyzer.setSessionActive()
    │
    ▼ eBPF 内核态（仅在 enabled 时统计）
a2dp_media.bpf.c: kprobe/tracepoint l2cap 流量 → BPF_MAP (per device 聚合)
    │
    ▼ 用户态每秒读
BtAudioAnalyzer.getStats()
    │
    ▼
BtAudioFusion.evaluate(transport, stats)
    │   ├── effective_active = active && bytes>min
    │   ├── suspected_stall  = active && gap>threshold
    │   └── 时间加权 Delay
    ▼
BtAudioQuality (升级版) → EventManager
```

### 4.8 线程调度与集成

**不新增线程**，严格复用现有线程：

| 工作内容 | 所属线程 | 调用点 | 频率 |
| -------- | -------- | ------ | ---- |
| 频段冲突 feed + detect | `network_quality_thread` | server.cpp 循环内 | 15s |
| 距离估算 + A2DP D-Bus 刷新 | 蓝牙监测线程 | `refreshDeviceStates()` 内 | 3s |
| eBPF map 读取 + 融合评估 | 蓝牙监测线程 | `refreshDeviceStates()` 内（Phase 2） | 3s |
| D-Bus PropertiesChanged 订阅 | 蓝牙监测线程 | `initialize()` 时加 match | 事件驱动 |
| 低频校准轮询 | 蓝牙监测线程 | 每 10-30s `GetAll` | 兜底 |

### 4.9 NetInfo 扩展

在 `net_info.hpp` 中新增字段，并扩展 `toJson()`/`fromJson()`/`isValid()`/`needsUpdate()`：

```cpp
class NetInfo {
    // ... 现有字段 ...
private:
    double bt_distance_ = -1.0;            // 蓝牙设备距离（米）
    std::string bt_audio_quality_;          // 蓝牙音频质量等级
    bool band_conflict_ = false;            // 是否检测到频段冲突
    double band_conflict_confidence_ = 0.0; // 频段冲突置信度
public:
    void setBtDistance(double d) { bt_distance_ = d; }
    double btDistance() const { return bt_distance_; }
    bool hasBtDistance() const { return bt_distance_ >= 0.0; }
    void setBtAudioQuality(const std::string& q) { bt_audio_quality_ = q; }
    const std::string& btAudioQuality() const { return bt_audio_quality_; }
    void setBandConflict(bool v) { band_conflict_ = v; }
    bool bandConflict() const { return band_conflict_; }
    void setBandConflictConfidence(double c) { band_conflict_confidence_ = c; }
    double bandConflictConfidence() const { return band_conflict_confidence_; }
};
```

### 4.10 事件集成

全部走 `EventManager` 统一路由：

```cpp
// 频段冲突
getEventManager().emitNetworkQualityChanged(
    "2.4GHz band conflict detected", conflictSuggestion, "band_conflict_detector");

// 音频质量变化
getEventManager().emitBluetoothDeviceChanged("Audio quality degraded: " + level, mac);

// 距离变化
getEventManager().emitBluetoothDeviceChanged("Device distance changed: " + distStr, mac);
```

---

## 5. 资源需求

### 5.1 人力资源

| 角色 | 人数 | 投入阶段 | 职责 |
| ---- | ---- | -------- | ---- |
| C++ 后端开发 | 1 | 全阶段 | 核心模块开发、集成 |
| eBPF 开发 | 1 | Phase 2 | eBPF 程序编写、挂点探测 |
| 测试工程师 | 1 | M4 | 集成测试、性能测试 |

> 注：C++ 后端开发与 eBPF 开发可由同一人兼任。

### 5.2 硬件/环境需求

| 资源 | 规格 | 用途 |
| ---- | ---- | ---- |
| 开发机 | Linux (内核 ≥ 5.10) | eBPF 开发与调试 |
| 蓝牙适配器 | 支持 BlueZ 5.x | 蓝牙功能测试 |
| 蓝牙音频设备 | 支持 A2DP | 音频质量监控测试 |
| Wi-Fi 网卡 | 2.4GHz 频段 | 频段冲突场景测试 |
| root 权限 | 开发机 | eBPF 程序加载 |

### 5.3 软件依赖

| 依赖 | 版本 | 用途 |
| ---- | ---- | ---- |
| libbpf | ≥ 1.0 | eBPF 程序加载与 CO-RE |
| clang | ≥ 14 | eBPF 程序编译（`-target bpf`） |
| BlueZ | ≥ 5.50 | D-Bus MediaTransport1 接口 |
| bpftool | ≥ 7.0 | vmlinux.h 生成 |
| libdbus-1 | 现有版本 | D-Bus 通信 |

---

## 6. 分阶段实施计划

### 6.1 总体策略

**三阶段交付，每阶段独立可上线，后续阶段为增强升级。**

| 阶段 | 内容 | 工期 | 交付价值 |
| ---- | ---- | ---- | -------- |
| Phase 1a（M1） | 频段冲突检测器 + `network_quality_thread` 接入 | 3-4 天 | 填补多协议协同分析空白，定位"干扰"场景 |
| Phase 1b（M2） | A2DP 纯 D-Bus 监控 + 设备距离估算 + NetInfo 扩展 | 3-4 天 | 应用层体验监控 + 空间感知 |
| Phase 2（M3） | eBPF 蓝牙流量采集 + D-Bus/eBPF 融合层 + 降级探测 | 5-7 天 | 升级音频监控为完整形态，可检测"active 但卡顿" |

**关键约束**：Phase 1 完成后即有完整可上线产品；Phase 2 是对音频监控的增强升级，其 eBPF 不可用时自动降级回 Phase 1b 的纯 D-Bus 形态。

### 6.2 详细任务分解

| 优先级 | 编号 | 任务 | 所属阶段 | 前置依赖 | 预估工时 |
| ------ | ---- | ---- | -------- | -------- | -------- |
| P0 | T1 | 新建 `band_conflict_detector.hpp/cpp`（Pearson + 基线 + 置信度） | Phase 1a | 无 | 1.5 天 |
| P0 | T2 | `network_quality_thread` 接入冲突检测，推送事件 | Phase 1a | T1 | 1.5 天 |
| P1 | T3 | 扩展 `NetInfo` 蓝牙字段 + 序列化（`toJson`/`fromJson`） | Phase 1b | 无 | 0.5 天 |
| P1 | T4 | BtMonitor 扩展 `refreshAudioTransports` + 评分算法 | Phase 1b | T3 | 1.5 天 |
| P2 | T5 | BtMonitor 距离估算 + 校准接口 | Phase 1b | T3 | 1 天 |
| P1 | T6 | 编写 `bpf/a2dp_media.bpf.c` + Makefile 规则 | Phase 2 | 无 | 1.5 天 |
| P1 | T7 | `bt_audio_analyzer`（加载/挂点探测/降级/map 读写） | Phase 2 | T6 | 1.5 天 |
| P1 | T8 | `bt_audio_fusion`（状态机 + 时间加权 + stall 检测） | Phase 2 | T7 | 1.5 天 |
| P1 | T9 | BtMonitor 接入融合层，D-Bus 事件驱动 BPF Map 开关 | Phase 2 | T4, T8 | 1 天 |
| P0 | T10 | 集成测试 + RAG 知识库补充诊断条目 | 全阶段 | T2, T4, T5, T9 | 1.5 天 |

### 6.3 模块划分与文件清单

#### Phase 1 新增/修改文件

| 类型 | 文件 | 职责 |
| ---- | ---- | ---- |
| **新增** | `server/include/band_conflict_detector.hpp` | 频段冲突检测器头文件 |
| **新增** | `server/src/band_conflict_detector.cpp` | 频段冲突检测器实现 |
| **修改** | `server/include/bt_monitor.hpp` | 新增 `BtAudioTransport`/`BtAudioQuality`/距离接口 |
| **修改** | `server/src/bt_monitor.cpp` | 新增 `refreshAudioTransports`/`estimateDistance` |
| **修改** | `server/include/net_info.hpp` | 新增蓝牙字段 |
| **修改** | `server/src/net_info.cpp` | 扩展序列化/反序列化/验证 |
| **修改** | `server/src/server.cpp` | `network_quality_thread` 接入冲突检测 |

#### Phase 2 新增/修改文件

| 类型 | 文件 | 职责 |
| ---- | ---- | ---- |
| **新增** | `server/src/bpf/a2dp_media.bpf.c` | 蓝牙 L2CAP/AVDTP 流量采集 eBPF 程序 |
| **新增** | `server/include/bt_audio_analyzer.hpp` | eBPF 蓝牙流量分析器头文件 |
| **新增** | `server/src/bt_audio_analyzer.cpp` | eBPF 蓝牙流量分析器实现 |
| **新增** | `server/include/bt_audio_fusion.hpp` | 融合层头文件 |
| **新增** | `server/src/bt_audio_fusion.cpp` | 融合层实现 |
| **修改** | `server/include/bt_monitor.hpp` | 接入融合层接口 |
| **修改** | `server/src/bt_monitor.cpp` | 接入融合层调用 |
| **修改** | `server/Makefile` | 新增 eBPF 对象编译规则 |

---

## 7. 风险管理策略

### 7.1 风险登记册

| 编号 | 风险描述 | 概率 | 影响 | 风险等级 | 应对策略 | 触发条件 | 责任人 |
| ---- | -------- | ---- | ---- | -------- | -------- | -------- | ------ |
| R1 | BlueZ 版本差异致 MediaTransport1 接口不可用 | 中 | 中 | 中 | **规避**：探测接口存在性，不可用时降级跳过 | 初始化时 `GetManagedObjects` 未找到 MediaTransport1 | 后端 |
| R2 | Pearson 样本不足致误判 | 中 | 高 | 高 | **规避**：最少 5 样本，不足时不输出结论 | 历史队列长度 < 5 | 后端 |
| R3 | 距离估算误差大（>50%） | 高 | 中 | 高 | **缓解**：输出"距离区间"而非精确值（如"1-3 米"） | 持续运行观察 | 后端 |
| R4 | 蓝牙适配器不支持 RSSI 上报 | 低 | 中 | 低 | **接受**：降级为仅监控连接状态 | RSSI 持续为 0 | 后端 |
| R5 | 频段冲突 Wi-Fi/蓝牙采样不同步 | 中 | 中 | 中 | **缓解**：时间戳对齐，容忍 2 秒内偏差 | 持续运行观察 | 后端 |
| R6 | eBPF 挂点内核升级失效 | 高 | 中 | 高 | **规避**：启动期探测 tracepoint→fentry→kprobe→纯 D-Bus；日志明示 | 内核升级后 attach 失败 | eBPF |
| R7 | 会话映射错误（D-Bus 路径 ↔ hci+BDADDR+CID） | 中 | 中 | 中 | **缓解**：首版按 `hci_index+BDADDR` 聚合为设备级，不虚假标 Transport 级 | 持续运行观察 | 后端 |
| R8 | PSM 0x0019 过滤含信令流量 | 中 | 低 | 低 | **缓解**：按 L2CAP CID 区分信令/媒体，无法区分时聚合到设备级 | 持续运行观察 | eBPF |
| R9 | LE Audio 不走 AVDTP（新蓝牙标准） | 中 | 中 | 中 | **接受**：文档标注不适用 LE Audio/BAP，第一版仅支持经典蓝牙 A2DP | 用户使用 LE Audio 设备 | 后端 |

### 7.2 降级策略

```cpp
// 频段冲突检测降级
if (wifiRssiHistory.size() < 5 || btRssiHistory.size() < 5) {
    return BandConflictResult{};  // 样本不足跳过
}

// A2DP 监控降级
if (!hasMediaTransportInterface()) {
    LOG_INFO(LogModule::BLUETOOTH, "MediaTransport1 not available, skip audio monitoring");
    return;
}

// eBPF 降级（Phase 2）
if (!btAudioAnalyzer.isAvailable()) {
    // 自动退化为纯 D-Bus（路线一），仍可输出基础音频质量
}

// 距离估算降级
if (rssiDbm == 0 || txPower == 0) {
    estimatedDistance = -1.0;  // 未知
    return;
}
```

---

## 8. 质量保障措施

### 8.1 测试策略

| 测试层级 | 方法 | 覆盖范围 | 验收标准 |
| -------- | ---- | -------- | -------- |
| 单元测试 | Pearson/距离/评分算法纯函数测试 | 所有算法函数 | Pearson 完全正相关时=1.0；1m 处距离≈1.0 |
| 单元测试 | `BandConflictDetector` 边界测试 | 空历史/满历史/单样本 | 不崩溃，返回空结果 |
| 单元测试 | `BtAudioQuality` 评分边界测试 | 0ms/100ms/500ms/5000ms Delay | 评分输出在 0-100 区间 |
| D-Bus 模拟 | `dbusmock` 模拟 BlueZ MediaTransport1 | 属性变化/接口缺失 | 状态变化被正确捕获；缺失时降级 |
| eBPF 降级测试 | 低内核版本或不支持蓝牙挂点的机器 | 挂点探测失败路径 | 自动退化为纯 D-Bus，启动日志明示 |
| 场景测试 | 手动 Wi-Fi 大流量下载 + 蓝牙播放 | 频段冲突检测 | 检测器置信度>50% 且输出建议 |
| 场景测试 | 播放音乐中移远设备制造卡顿（Phase 2） | eBPF 融合层 | `suspectedStall=true`，有效占比下降 |
| 集成测试 | 完整运行 server，观察日志与 D-Bus 信号 | 全链路 | 事件经 EventManager 正确路由 |
| 性能测试 | 长时间运行（>24h）监控内存/CPU | 资源泄漏 | 内存稳定，CPU 无显著增长 |

### 8.2 代码审查检查点

- [ ] 所有新增类遵循 RAII 原则，资源在析构函数中正确释放
- [ ] 所有跨线程共享数据有适当的互斥锁保护
- [ ] eBPF 程序在进程退出时正确 detach 和清理
- [ ] 所有 D-Bus 消息发送后正确 `unref`
- [ ] 日志使用正确的 `LogModule` 分类（BLUETOOTH / WEAK_MGR）
- [ ] 与现有代码风格一致（命名、缩进、注释密度）
- [ ] 编译通过 `-Wall -Wextra -Wpedantic` 无新增警告

### 8.3 持续集成

- 每次提交触发 `make` 全量编译
- 单元测试纳入 CI 流水线
- eBPF 程序编译验证（`clang -target bpf -c`）

---

## 9. 验收标准

### 9.1 M1 验收标准（Phase 1a：频段冲突检测）

| 编号 | 验收项 | 验证方法 | 通过标准 |
| ---- | ------ | -------- | -------- |
| AC1.1 | `BandConflictDetector` 正确实现 Pearson 计算 | 单元测试 | 完全正相关输入返回 1.0，完全负相关返回 -1.0 |
| AC1.2 | 样本不足时返回空结果 | 单元测试 | 样本 < 5 时 `detected=false` |
| AC1.3 | `network_quality_thread` 周期调用检测器 | 运行日志 | 日志中每 15s 出现冲突检测记录 |
| AC1.4 | 手动制造 Wi-Fi/蓝牙干扰，检测器识别 | 手动测试 | 置信度 > 50%，日志输出处置建议 |
| AC1.5 | 事件经 EventManager 正确推送 | D-Bus 信号监控 | `dbus-monitor` 捕获到 `NetworkQualityChanged` 信号 |

### 9.2 M2 验收标准（Phase 1b：A2DP D-Bus + 距离）

| 编号 | 验收项 | 验证方法 | 通过标准 |
| ---- | ------ | -------- | -------- |
| AC2.1 | 播放蓝牙音乐时能看到音频延迟和质量评分 | 运行日志 | 日志输出 `BtAudioQuality` 含 delay/score/level |
| AC2.2 | MediaTransport1 不可用时正确降级 | 模拟测试 | 日志输出 "MediaTransport1 not available" |
| AC2.3 | 设备移动时距离估算值合理变化 | 手动测试 | 1m 处估算值 0.5-2m，5m 处估算值 2-8m |
| AC2.4 | `calibrateDistance` 接口可正常调用 | 手动测试 | 校准后距离估算值更接近真实值 |
| AC2.5 | NetInfo JSON 序列化包含新字段 | 单元测试 | `toJson()` 输出含 `bt_distance`/`bt_audio_quality`/`band_conflict` |

### 9.3 M3 验收标准（Phase 2：eBPF 融合层）

| 编号 | 验收项 | 验证方法 | 通过标准 |
| ---- | ------ | -------- | -------- |
| AC3.1 | eBPF 程序在支持的内核上成功 attach | 运行日志 | 日志输出 "eBPF mode: enabled" + hook 名称 |
| AC3.2 | 内核不支持蓝牙挂点时自动降级 | 低内核环境 | 日志输出 "eBPF mode: unavailable, fallback: D-Bus only" |
| AC3.3 | BPF Map 控制开关正确工作 | 运行日志 | `active` 时 enabled=1，`idle` 时 enabled=0 |
| AC3.4 | 播放音乐中制造卡顿，`suspectedStall` 为 true | 手动测试 | 设备移远至断连边缘，融合层输出 `suspectedStall=true` |
| AC3.5 | 正常播放时 `effectiveRatio` > 80% | 手动测试 | 近距离稳定播放 5 分钟，有效占比 > 80% |
| AC3.6 | 降级回纯 D-Bus 时仍能输出基础音频质量 | 模拟降级 | eBPF 不可用时 `BtAudioQuality` 仍基于 D-Bus 数据输出 |

### 9.4 M4 验收标准（集成测试 + RAG）

| 编号 | 验收项 | 验证方法 | 通过标准 |
| ---- | ------ | -------- | -------- |
| AC4.1 | 全链路事件正确路由 | 集成测试 | 所有事件类型经 EventManager 正确分发 |
| AC4.2 | 24 小时无内存泄漏、无崩溃 | 长时间运行 | 内存稳定，进程无 coredump |
| AC4.3 | RAG 能回答"频段冲突"诊断 | 查询测试 | 输入"蓝牙卡顿"，RAG 返回频段冲突相关建议 |
| AC4.4 | RAG 能回答"音频卡顿"诊断 | 查询测试 | 输入"蓝牙音频延迟高"，RAG 返回编解码器/距离建议 |

---

## 10. 项目时间线

### 10.1 甘特图

```
Week 1                Week 2                Week 3
│ M T W T F S S │ M T W T F S S │ M T W T F S S │
├───────────────┼───────────────┼───────────────┤
│████████████   │               │               │  Phase 1a (M1)
│ T1  T2        │               │               │  频段冲突检测
│               │████████████   │               │  Phase 1b (M2)
│               │ T3 T4 T5      │               │  A2DP D-Bus + 距离
│               │               │███████████████│  Phase 2 (M3)
│               │               │ T6 T7 T8 T9   │  eBPF 融合层
│               │               │             ██│  M4: 集成测试 + RAG
└───────────────┴───────────────┴───────────────┘
```

### 10.2 里程碑节点

| 里程碑 | 日期（从启动起算） | 交付物 | 验收方式 |
| ------ | ----------------- | ------ | -------- |
| M1 | D+4 | 频段冲突检测器上线 | AC1.1 ~ AC1.5 |
| M2 | D+8 | A2DP D-Bus 监控 + 距离估算 | AC2.1 ~ AC2.5 |
| M3 | D+15 | eBPF 融合层上线 | AC3.1 ~ AC3.6 |
| M4 | D+17 | 集成测试通过 + RAG 条目 | AC4.1 ~ AC4.4 |

### 10.3 关键路径

```
T1 → T2 → M1 → T3 → T4 → T5 → M2 → T6 → T7 → T8 → T9 → M3 → T10 → M4
                                       ↘ T6（可与 M2 并行开始）
```

- **关键路径**：T1 → T2 → T3 → T4 → T8 → T9 → T10（总工期约 17 天）
- **可并行**：T6（eBPF 程序编写）可在 M2 完成后与 T7 并行，无需等待 M2 全部完成

---

## 附录 A：核心设计原则

本方案遵循以下核心设计原则：

1. **不新增线程**：复用蓝牙监测线程（3s）和网络质量评估线程（15s），降低资源开销
2. **统一事件路由**：所有新事件通过 `EventManager` 推送，保持事件体系一致
3. **降级容错**：与项目现有降级模式一致，eBPF 不可用时自动退化为纯 D-Bus
4. **数据复用**：最大化利用现有 RSSI 历史数据，避免重复采集
5. **分层职责**：D-Bus 管语义状态、eBPF 管数据路径实证、融合层管判断结论
6. **挂点探测优先级**：tracepoint > fentry > kprobe > 纯 D-Bus，启动日志明示降级原因
7. **ABI 兼容意识**：eBPF 启动时探测可用挂点，不在代码中硬编码内核函数假设

## 附录 B：参考文档

- BlueZ D-Bus API 文档：https://git.kernel.org/pub/scm/bluetooth/bluez.git/tree/doc
- libbpf CO-RE 指南：https://nakryiko.com/posts/bpf-portability-and-co-re/
- Linux 内核蓝牙子系统文档：Documentation/networking/bluetooth.txt
- 项目现有 eBPF 实现：`server/src/flow_rate.bpf.c`、`server/src/net_traffic.cpp`