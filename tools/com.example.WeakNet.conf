<!DOCTYPE busconfig PUBLIC "-//freedesktop//DTD D-BUS Bus Configuration 1.0//EN"
 "http://www.freedesktop.org/standards/dbus/1.0/busconfig.dtd">
<busconfig>
  <!-- WeakNet 网络诊断服务策略：服务端以 root 注册 com.example.WeakNet，
       本机用户（含 root）可调用其方法并接收其信号。
       安装位置：/etc/dbus-1/system.d/com.example.WeakNet.conf -->
  <policy context="default">
    <!-- 允许任何本地用户注册/替换服务名（服务端以 root 运行） -->
    <allow own="com.example.WeakNet"/>
    <!-- 允许向总线上的任意服务发送方法调用（含 com.example.WeakNet 与 org.bluez） -->
    <allow send_destination="*"/>
    <!-- 允许接收来自任意服务的信号（如 NetworkQualityChanged / BluetoothDeviceChanged） -->
    <allow receive_sender="*"/>
  </policy>
</busconfig>
